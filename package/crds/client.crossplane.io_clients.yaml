---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: clients.client.crossplane.io
spec:
  group: client.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - auth0
    kind: Client
    listKind: ClientList
    plural: clients
    singular: client
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Client is the Schema for the Clients API. With this resource,
          you can set up applications that use Auth0 for authentication and configure
          allowed callback URLs and secrets for these applications.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClientSpec defines the desired state of Client
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  addons:
                    description: |-
                      (Block List, Max: 1) Addons enabled for this client and their associated configurations. (see below for nested schema)
                      Addons enabled for this client and their associated configurations.
                    items:
                      properties:
                        aws:
                          description: |-
                            (Block List, Max: 1) AWS Addon configuration. (see below for nested schema)
                            AWS Addon configuration.
                          items:
                            properties:
                              lifetimeInSeconds:
                                description: |-
                                  (Number) AWS token lifetime in seconds.
                                  AWS token lifetime in seconds.
                                type: number
                              principal:
                                description: |-
                                  provider/idpname.
                                  AWS principal ARN, for example `arn:aws:iam::010616021751:saml-provider/idpname`.
                                type: string
                              role:
                                description: |-
                                  (String) AWS role ARN, for example arn:aws:iam::010616021751:role/foo.
                                  AWS role ARN, for example `arn:aws:iam::010616021751:role/foo`.
                                type: string
                            type: object
                          type: array
                        azureBlob:
                          description: |-
                            (Block List, Max: 1) Azure Blob Storage Addon configuration. (see below for nested schema)
                            Azure Blob Storage Addon configuration.
                          items:
                            properties:
                              accountName:
                                description: |-
                                  org.blob.core.windows.net would be the account name acme-org.
                                  Your Azure storage account name. Usually first segment in your Azure storage URL, for example `https://acme-org.blob.core.windows.net` would be the account name `acme-org`.
                                type: string
                              blobDelete:
                                description: |-
                                  (Boolean) Indicates if the issued token has permission to delete the blob.
                                  Indicates if the issued token has permission to delete the blob.
                                type: boolean
                              blobName:
                                description: |-
                                  blob. If blank the computed SAS will apply to the entire storage container.
                                  Entity to request a token for, such as `my-blob`. If blank the computed SAS will apply to the entire storage container.
                                type: string
                              blobRead:
                                description: |-
                                  (Boolean) Indicates if the issued token has permission to read the content, properties, metadata and block list. Use the blob as the source of a copy operation.
                                  Indicates if the issued token has permission to read the content, properties, metadata and block list. Use the blob as the source of a copy operation.
                                type: boolean
                              blobWrite:
                                description: |-
                                  (Boolean) Indicates if the issued token has permission to create or write content, properties, metadata, or block list. Snapshot or lease the blob. Resize the blob (page blob only). Use the blob as the destination of a copy operation within the same account.
                                  Indicates if the issued token has permission to create or write content, properties, metadata, or block list. Snapshot or lease the blob. Resize the blob (page blob only). Use the blob as the destination of a copy operation within the same account.
                                type: boolean
                              containerDelete:
                                description: |-
                                  (Boolean) Indicates if issued token has permission to delete any blob in the container.
                                  Indicates if issued token has permission to delete any blob in the container.
                                type: boolean
                              containerList:
                                description: |-
                                  (Boolean) Indicates if the issued token has permission to list blobs in the container.
                                  Indicates if the issued token has permission to list blobs in the container.
                                type: boolean
                              containerName:
                                description: |-
                                  container.
                                  Container to request a token for, such as `my-container`.
                                type: string
                              containerRead:
                                description: |-
                                  (Boolean) Indicates if the issued token has permission to read the content, properties, metadata or block list of any blob in the container. Use any blob in the container as the source of a copy operation.
                                  Indicates if the issued token has permission to read the content, properties, metadata or block list of any blob in the container. Use any blob in the container as the source of a copy operation.
                                type: boolean
                              containerWrite:
                                description: |-
                                  (Boolean) Indicates that for any blob in the container if the issued token has permission to create or write content, properties, metadata, or block list. Snapshot or lease the blob. Resize the blob (page blob only). Use the blob as the destination of a copy operation within the same account.
                                  Indicates that for any blob in the container if the issued token has permission to create or write content, properties, metadata, or block list. Snapshot or lease the blob. Resize the blob (page blob only). Use the blob as the destination of a copy operation within the same account.
                                type: boolean
                              expiration:
                                description: |-
                                  (Number) Expiration in minutes for the generated token (default of 5 minutes).
                                  Expiration in minutes for the generated token (default of 5 minutes).
                                type: number
                              signedIdentifier:
                                description: |-
                                  (String) Shared access policy identifier defined in your storage account resource.
                                  Shared access policy identifier defined in your storage account resource.
                                type: string
                              storageAccessKeySecretRef:
                                description: |-
                                  (String, Sensitive) Access key associated with this storage account.
                                  Access key associated with this storage account.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          type: array
                        azureSb:
                          description: |-
                            (Block List, Max: 1) Azure Storage Bus Addon configuration. (see below for nested schema)
                            Azure Storage Bus Addon configuration.
                          items:
                            properties:
                              entityPath:
                                description: |-
                                  queue.
                                  Entity you want to request a token for, such as `my-queue`.
                                type: string
                              expiration:
                                description: |-
                                  (Number) Expiration in minutes for the generated token (default of 5 minutes).
                                  Optional expiration in minutes for the generated token. Defaults to 5 minutes.
                                type: number
                              namespace:
                                description: |-
                                  org.servicebus.windows.net would be acme-org).
                                  Your Azure Service Bus namespace. Usually the first segment of your Service Bus URL (for example `https://acme-org.servicebus.windows.net` would be `acme-org`).
                                type: string
                              sasKeyName:
                                description: |-
                                  (String) Your shared access policy name defined in your Service Bus entity.
                                  Your shared access policy name defined in your Service Bus entity.
                                type: string
                              sasKeySecretRef:
                                description: |-
                                  (String, Sensitive) Primary Key associated with your shared access policy.
                                  Primary Key associated with your shared access policy.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          type: array
                        box:
                          description: |-
                            (Block List, Max: 1) Box SSO indicator (no configuration settings needed for Box SSO). (see below for nested schema)
                            Box SSO indicator (no configuration settings needed for Box SSO).
                          items:
                            type: object
                          type: array
                        cloudbees:
                          description: |-
                            (Block List, Max: 1) CloudBees SSO indicator (no configuration settings needed for CloudBees SSO). (see below for nested schema)
                            CloudBees SSO indicator (no configuration settings needed for CloudBees SSO).
                          items:
                            type: object
                          type: array
                        concur:
                          description: |-
                            (Block List, Max: 1) Concur SSO indicator (no configuration settings needed for Concur SSO). (see below for nested schema)
                            Concur SSO indicator (no configuration settings needed for Concur SSO).
                          items:
                            type: object
                          type: array
                        dropbox:
                          description: |-
                            (Block List, Max: 1) Dropbox SSO indicator (no configuration settings needed for Dropbox SSO). (see below for nested schema)
                            Dropbox SSO indicator (no configuration settings needed for Dropbox SSO).
                          items:
                            type: object
                          type: array
                        echosign:
                          description: |-
                            (Block List, Max: 1) Adobe EchoSign SSO configuration. (see below for nested schema)
                            Adobe EchoSign SSO configuration.
                          items:
                            properties:
                              domain:
                                description: |-
                                  org.echosign.com would be acme-org.
                                  Your custom domain found in your EchoSign URL, for example `https://acme-org.echosign.com` would be `acme-org`.
                                type: string
                            type: object
                          type: array
                        egnyte:
                          description: |-
                            (Block List, Max: 1) Egnyte SSO configuration. (see below for nested schema)
                            Egnyte SSO configuration.
                          items:
                            properties:
                              domain:
                                description: |-
                                  org.echosign.com would be acme-org.
                                  Your custom domain found in your Egnyte URL, for example `https://acme-org.echosign.com` would be `acme-org`.
                                type: string
                            type: object
                          type: array
                        firebase:
                          description: |-
                            (Block List, Max: 1) Google Firebase addon configuration. (see below for nested schema)
                            Google Firebase addon configuration.
                          items:
                            properties:
                              clientEmail:
                                description: |-
                                  (String) ID of the Service Account you have created (shown as client_email in the generated JSON file, SDK v3+ tokens only).
                                  ID of the Service Account you have created (shown as `client_email` in the generated JSON file, SDK v3+ tokens only).
                                type: string
                              lifetimeInSeconds:
                                description: |-
                                  (Number) AWS token lifetime in seconds.
                                  Optional expiration in seconds for the generated token. Defaults to 3600 seconds (SDK v3+ tokens only).
                                type: number
                              privateKeyIdSecretRef:
                                description: |-
                                  (String, Sensitive) Optional ID of the private key to obtain the kid header claim from the issued token (SDK v3+ tokens only).
                                  Optional ID of the private key to obtain the `kid` header claim from the issued token (SDK v3+ tokens only).
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              privateKeySecretRef:
                                description: |-
                                  (String, Sensitive) Private Key for signing the token (SDK v3+ tokens only).
                                  Private Key for signing the token (SDK v3+ tokens only).
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              secretSecretRef:
                                description: |-
                                  (String, Sensitive) Google Firebase Secret. (SDK v2 only).
                                  Google Firebase Secret. (SDK v2 only).
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          type: array
                        layer:
                          description: |-
                            (Block List, Max: 1) Layer addon configuration. (see below for nested schema)
                            Layer addon configuration.
                          items:
                            properties:
                              expiration:
                                description: |-
                                  (Number) Expiration in minutes for the generated token (default of 5 minutes).
                                  Optional expiration in minutes for the generated token. Defaults to 5 minutes.
                                type: number
                              keyIdSecretRef:
                                description: |-
                                  (String, Sensitive) Authentication Key identifier used to sign the Layer token.
                                  Authentication Key identifier used to sign the Layer token.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              principal:
                                description: |-
                                  provider/idpname.
                                  Name of the property used as the unique user ID in Layer. If not specified `user_id` is used.
                                type: string
                              privateKeySecretRef:
                                description: |-
                                  (String, Sensitive) Private Key for signing the token (SDK v3+ tokens only).
                                  Private key for signing the Layer token.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              providerId:
                                description: |-
                                  (String) Provider ID of your Layer account.
                                  Provider ID of your Layer account.
                                type: string
                            type: object
                          type: array
                        mscrm:
                          description: |-
                            (Block List, Max: 1) Microsoft Dynamics CRM SSO configuration. (see below for nested schema)
                            Microsoft Dynamics CRM SSO configuration.
                          items:
                            properties:
                              url:
                                description: |-
                                  (String) Microsoft Dynamics CRM application URL.
                                  Microsoft Dynamics CRM application URL.
                                type: string
                            type: object
                          type: array
                        newrelic:
                          description: |-
                            (Block List, Max: 1) New Relic SSO configuration. (see below for nested schema)
                            New Relic SSO configuration.
                          items:
                            properties:
                              account:
                                description: |-
                                  (String) Your New Relic Account ID found in your New Relic URL after the /accounts/ path, for example https://rpm.newrelic.com/accounts/123456/query would be 123456.
                                  Your New Relic Account ID found in your New Relic URL after the `/accounts/` path, for example `https://rpm.newrelic.com/accounts/123456/query` would be `123456`.
                                type: string
                            type: object
                          type: array
                        office365:
                          description: |-
                            (Block List, Max: 1) Microsoft Office 365 SSO configuration. (see below for nested schema)
                            Microsoft Office 365 SSO configuration.
                          items:
                            properties:
                              connection:
                                description: |-
                                  configured Office 365 tenant.
                                  Optional Auth0 database connection for testing an already-configured Office 365 tenant.
                                type: string
                              domain:
                                description: |-
                                  org.echosign.com would be acme-org.
                                  Your Office 365 domain name, for example `acme-org.com`.
                                type: string
                            type: object
                          type: array
                        rms:
                          description: |-
                            (Block List, Max: 1) Active Directory Rights Management Service SSO configuration. (see below for nested schema)
                            Active Directory Rights Management Service SSO configuration.
                          items:
                            properties:
                              url:
                                description: |-
                                  (String) Microsoft Dynamics CRM application URL.
                                  URL of your Rights Management Server. It can be internal or external, but users will have to be able to reach it.
                                type: string
                            type: object
                          type: array
                        salesforce:
                          description: |-
                            (Block List, Max: 1) Salesforce SSO configuration. (see below for nested schema)
                            Salesforce SSO configuration.
                          items:
                            properties:
                              entityId:
                                description: |-
                                  org.com.
                                  Arbitrary logical URL that identifies the Saleforce resource, for example `https://acme-org.com`.
                                type: string
                            type: object
                          type: array
                        salesforceApi:
                          description: |-
                            (Block List, Max: 1) Salesforce API addon configuration. (see below for nested schema)
                            Salesforce API addon configuration.
                          items:
                            properties:
                              clientIdSecretRef:
                                description: |-
                                  (String) The ID of the client.
                                  Consumer Key assigned by Salesforce to the Connected App.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              communityName:
                                description: |-
                                  (String) Community name.
                                  Community name.
                                type: string
                              communityUrlSection:
                                description: |-
                                  (String) Community URL section.
                                  Community URL section.
                                type: string
                              principalSecretRef:
                                description: |-
                                  provider/idpname.
                                  Name of the property in the user object that maps to a Salesforce username, for example `email`.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          type: array
                        salesforceSandboxApi:
                          description: |-
                            (Block List, Max: 1) Salesforce Sandbox addon configuration. (see below for nested schema)
                            Salesforce Sandbox addon configuration.
                          items:
                            properties:
                              clientIdSecretRef:
                                description: |-
                                  (String) The ID of the client.
                                  Consumer Key assigned by Salesforce to the Connected App.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              communityName:
                                description: |-
                                  (String) Community name.
                                  Community name.
                                type: string
                              communityUrlSection:
                                description: |-
                                  (String) Community URL section.
                                  Community URL section.
                                type: string
                              principalSecretRef:
                                description: |-
                                  provider/idpname.
                                  Name of the property in the user object that maps to a Salesforce username, for example `email`.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          type: array
                        samlp:
                          description: |-
                            on. (see below for nested schema)
                            Configuration settings for a SAML add-on.
                          items:
                            properties:
                              audience:
                                description: |-
                                  (String) Audience of the SAML Assertion. Default will be the Issuer on SAMLRequest.
                                  Audience of the SAML Assertion. Default will be the Issuer on SAMLRequest.
                                type: string
                              authnContextClassRef:
                                description: |-
                                  (String) Class reference of the authentication context.
                                  Class reference of the authentication context.
                                type: string
                              binding:
                                description: |-
                                  (String) Protocol binding used for SAML logout responses.
                                  Protocol binding used for SAML logout responses.
                                type: string
                              createUpnClaim:
                                description: |-
                                  (Boolean) Indicates whether a UPN claim should be created. Defaults to true.
                                  Indicates whether a UPN claim should be created. Defaults to `true`.
                                type: boolean
                              destination:
                                description: |-
                                  (String) Destination of the SAML Response. If not specified, it will be AssertionConsumerUrl of SAMLRequest or callback URL if there was no SAMLRequest.
                                  Destination of the SAML Response. If not specified, it will be `AssertionConsumerUrl` of SAMLRequest or callback URL if there was no SAMLRequest.
                                type: string
                              digestAlgorithm:
                                description: |-
                                  (String) Algorithm used to calculate the digest of the SAML Assertion or response. Options include sha1 and sha256. Defaults to sha1.
                                  Algorithm used to calculate the digest of the SAML Assertion or response. Options include `sha1` and `sha256`. Defaults to `sha1`.
                                type: string
                              flexibleMappings:
                                description: |-
                                  (String) This is a supporting attribute to mappings field.Please note this is an experimental field. It should only be used when needed to send a map with keys as slices.
                                  This is a supporting attribute to `mappings` field.Please note this is an experimental field. It should only be used when needed to send a map with keys as slices.
                                type: string
                              includeAttributeNameFormat:
                                description: |-
                                  (Boolean) Indicates whether or not we should infer the NameFormat based on the attribute name. If set to false, the attribute NameFormat is not set in the assertion. Defaults to true.
                                  Indicates whether or not we should infer the NameFormat based on the attribute name. If set to `false`, the attribute NameFormat is not set in the assertion. Defaults to `true`.
                                type: boolean
                              issuer:
                                description: |-
                                  (String) Issuer of the SAML Assertion.
                                  Issuer of the SAML Assertion.
                                type: string
                              lifetimeInSeconds:
                                description: |-
                                  (Number) AWS token lifetime in seconds.
                                  Number of seconds during which the token is valid. Defaults to `3600` seconds.
                                type: number
                              logout:
                                description: |-
                                  (Block List, Max: 1) Configuration settings for logout. (see below for nested schema)
                                  Configuration settings for logout.
                                items:
                                  properties:
                                    callback:
                                      description: |-
                                        (String) The service provider (client application)'s Single Logout Service URL, where Auth0 will send logout requests and responses.
                                        The service provider (client application)'s Single Logout Service URL, where Auth0 will send logout requests and responses.
                                      type: string
                                    sloEnabled:
                                      description: |-
                                        (Boolean) Controls whether Auth0 should notify service providers of session termination.
                                        Controls whether Auth0 should notify service providers of session termination.
                                      type: boolean
                                  type: object
                                type: array
                              mapIdentities:
                                description: |-
                                  (Boolean) Indicates whether or not to add additional identity information in the token, such as the provider used and the access_token, if available. Defaults to true.
                                  Indicates whether or not to add additional identity information in the token, such as the provider used and the `access_token`, if available. Defaults to `true`.
                                type: boolean
                              mapUnknownClaimsAsIs:
                                description: |-
                                  (Boolean) Indicates whether to add a prefix of http://schema.auth0.com to any claims that are not mapped to the common profile when passed through in the output assertion. Defaults to false.
                                  Indicates whether to add a prefix of `http://schema.auth0.com` to any claims that are not mapped to the common profile when passed through in the output assertion. Defaults to `false`.
                                type: boolean
                              mappings:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Mappings between the Auth0 user profile property name (name) and the output attributes on the SAML attribute in the assertion (value).
                                  Mappings between the Auth0 user profile property name (`name`) and the output attributes on the SAML attribute in the assertion (`value`).
                                type: object
                                x-kubernetes-map-type: granular
                              nameIdentifierFormat:
                                description: |-
                                  format:unspecified.
                                  Format of the name identifier. Defaults to `urn:oasis:names:tc:SAML:1.1:nameid-format:unspecified`.
                                type: string
                              nameIdentifierProbes:
                                description: |-
                                  (List of String) Attributes that can be used for Subject/NameID. Auth0 will try each of the attributes of this array in order and use the first value it finds.
                                  Attributes that can be used for Subject/NameID. Auth0 will try each of the attributes of this array in order and use the first value it finds.
                                items:
                                  type: string
                                type: array
                              passthroughClaimsWithNoMapping:
                                description: |-
                                  (Boolean) Indicates whether or not to passthrough claims that are not mapped to the common profile in the output assertion. Defaults to true.
                                  Indicates whether or not to passthrough claims that are not mapped to the common profile in the output assertion. Defaults to `true`.
                                type: boolean
                              recipient:
                                description: |-
                                  (String) Recipient of the SAML Assertion (SubjectConfirmationData). Default is AssertionConsumerUrl on SAMLRequest or callback URL if no SAMLRequest was sent.
                                  Recipient of the SAML Assertion (SubjectConfirmationData). Default is `AssertionConsumerUrl` on SAMLRequest or callback URL if no SAMLRequest was sent.
                                type: string
                              signResponse:
                                description: |-
                                  (Boolean) Indicates whether or not the SAML Response should be signed instead of the SAML Assertion.
                                  Indicates whether or not the SAML Response should be signed instead of the SAML Assertion.
                                type: boolean
                              signatureAlgorithm:
                                description: |-
                                  sha1 and rsa-sha256. Defaults to rsa-sha1.
                                  Algorithm used to sign the SAML Assertion or response. Options include `rsa-sha1` and `rsa-sha256`. Defaults to `rsa-sha1`.
                                type: string
                              signingCert:
                                type: string
                              typedAttributes:
                                description: |-
                                  (Boolean) Indicates whether or not we should infer the xs:type of the element. Types include xs:string, xs:boolean, xs:double, and xs:anyType. When set to false, all xs:type are xs:anyType. Defaults to true.
                                  Indicates whether or not we should infer the `xs:type` of the element. Types include `xs:string`, `xs:boolean`, `xs:double`, and `xs:anyType`. When set to `false`, all `xs:type` are `xs:anyType`. Defaults to `true`.
                                type: boolean
                            type: object
                          type: array
                        sapApi:
                          description: |-
                            (Block List, Max: 1) SAP API addon configuration. (see below for nested schema)
                            SAP API addon configuration.
                          items:
                            properties:
                              clientId:
                                description: |-
                                  (String) The ID of the client.
                                  If activated in the OAuth 2.0 client configuration (transaction `SOAUTH2) the SAML attribute `client_id` must be set and equal the `client_id` form parameter of the access token request.
                                type: string
                              nameIdentifierFormat:
                                description: |-
                                  format:unspecified.
                                  NameID element of the Subject which can be used to express the user's identity. Defaults to `urn:oasis:names:tc:SAML:1.1:nameid-format:unspecified`.
                                type: string
                              scope:
                                description: |-
                                  (String) Requested scope for SAP APIs.
                                  Requested scope for SAP APIs.
                                type: string
                              servicePasswordSecretRef:
                                description: |-
                                  (String, Sensitive) Service account password to use to authenticate API calls to the token endpoint.
                                  Service account password to use to authenticate API calls to the token endpoint.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              tokenEndpointUrl:
                                description: |-
                                  (String) The OAuth2 token endpoint URL of your SAP OData server.
                                  The OAuth2 token endpoint URL of your SAP OData server.
                                type: string
                              usernameAttribute:
                                description: |-
                                  (String) Name of the property in the user object that maps to a SAP username, for example email.
                                  Name of the property in the user object that maps to a SAP username, for example `email`.
                                type: string
                            type: object
                          type: array
                        sentry:
                          description: |-
                            (Block List, Max: 1) Sentry SSO configuration. (see below for nested schema)
                            Sentry SSO configuration.
                          items:
                            properties:
                              baseUrl:
                                description: |-
                                  (String) URL prefix only if running Sentry Community Edition, otherwise leave empty.
                                  URL prefix only if running Sentry Community Edition, otherwise leave empty.
                                type: string
                              orgSlug:
                                description: |-
                                  org/ would be acme-org.
                                  Generated slug for your Sentry organization. Found in your Sentry URL, for example `https://sentry.acme.com/acme-org/` would be `acme-org`.
                                type: string
                            type: object
                          type: array
                        sharepoint:
                          description: |-
                            (Block List, Max: 1) SharePoint SSO configuration. (see below for nested schema)
                            SharePoint SSO configuration.
                          items:
                            properties:
                              externalUrl:
                                description: |-
                                  (List of String) External SharePoint application URLs if exposed to the Internet.
                                  External SharePoint application URLs if exposed to the Internet.
                                items:
                                  type: string
                                type: array
                              url:
                                description: |-
                                  (String) Microsoft Dynamics CRM application URL.
                                  Internal SharePoint application URL.
                                type: string
                            type: object
                          type: array
                        slack:
                          description: |-
                            org.slack.com would be acme-org. (see below for nested schema)
                            Slack team or workspace name usually first segment in your Slack URL, for example `https://acme-org.slack.com` would be `acme-org`.
                          items:
                            properties:
                              team:
                                description: |-
                                  (String) Slack team name.
                                  Slack team name.
                                type: string
                            type: object
                          type: array
                        springcm:
                          description: |-
                            (Block List, Max: 1) SpringCM SSO configuration. (see below for nested schema)
                            SpringCM SSO configuration.
                          items:
                            properties:
                              acsUrl:
                                description: |-
                                  (String) SpringCM ACS URL, for example https://na11.springcm.com/atlas/sso/SSOEndpoint.ashx.
                                  SpringCM ACS URL, for example `https://na11.springcm.com/atlas/sso/SSOEndpoint.ashx`.
                                type: string
                            type: object
                          type: array
                        ssoIntegration:
                          description: |-
                            (Block List, Max: 1) Generic SSO configuration. (see below for nested schema)
                            Generic SSO configuration.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) Name of the client.
                                  SSO integration name.
                                type: string
                              version:
                                description: |-
                                  (String) SSO integration version installed.
                                  SSO integration version installed.
                                type: string
                            type: object
                          type: array
                        wams:
                          description: |-
                            (Block List, Max: 1) Windows Azure Mobile Services addon configuration. (see below for nested schema)
                            Windows Azure Mobile Services addon configuration.
                          items:
                            properties:
                              masterKeySecretRef:
                                description: |-
                                  (String, Sensitive) Your master key for Windows Azure Mobile Services.
                                  Your master key for Windows Azure Mobile Services.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          type: array
                        wsfed:
                          description: |-
                            Fed (WIF) addon indicator. Actual configuration is stored in callback and client_aliases properties on the client. (see below for nested schema)
                            WS-Fed (WIF) addon indicator. Actual configuration is stored in `callback` and `client_aliases` properties on the client.
                          items:
                            type: object
                          type: array
                        zendesk:
                          description: |-
                            (Block List, Max: 1) Zendesk SSO configuration. (see below for nested schema)
                            Zendesk SSO configuration.
                          items:
                            properties:
                              accountName:
                                description: |-
                                  org.blob.core.windows.net would be the account name acme-org.
                                  Zendesk account name. Usually the first segment in your Zendesk URL, for example `https://acme-org.zendesk.com` would be `acme-org`.
                                type: string
                            type: object
                          type: array
                        zoom:
                          description: |-
                            (Block List, Max: 1) Zoom SSO configuration. (see below for nested schema)
                            Zoom SSO configuration.
                          items:
                            properties:
                              account:
                                description: |-
                                  (String) Your New Relic Account ID found in your New Relic URL after the /accounts/ path, for example https://rpm.newrelic.com/accounts/123456/query would be 123456.
                                  Zoom account name. Usually the first segment of your Zoom URL, for example `https://acme-org.zoom.us` would be `acme-org`.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  allowedClients:
                    description: |-
                      (List of String) List of applications ID's that will be allowed to make delegation request. By default, all applications will be allowed.
                      List of applications ID's that will be allowed to make delegation request. By default, all applications will be allowed.
                    items:
                      type: string
                    type: array
                  allowedLogoutUrls:
                    description: |-
                      (List of String) URLs that Auth0 may redirect to after logout.
                      URLs that Auth0 may redirect to after logout.
                    items:
                      type: string
                    type: array
                  allowedOrigins:
                    description: |-
                      origin resource sharing. By default, all your callback URLs will be allowed.
                      URLs that represent valid origins for cross-origin resource sharing. By default, all your callback URLs will be allowed.
                    items:
                      type: string
                    type: array
                  appType:
                    description: |-
                      (String) Type of application the client represents. Possible values are: native, spa, regular_web, non_interactive, sso_integration. Specific SSO integrations types accepted as well are: rms, box, cloudbees, concur, dropbox, mscrm, echosign, egnyte, newrelic, office365, salesforce, sentry, sharepoint, slack, springcm, zendesk, zoom.
                      Type of application the client represents. Possible values are: `native`, `spa`, `regular_web`, `non_interactive`, `sso_integration`. Specific SSO integrations types accepted as well are: `rms`, `box`, `cloudbees`, `concur`, `dropbox`, `mscrm`, `echosign`, `egnyte`, `newrelic`, `office365`, `salesforce`, `sentry`, `sharepoint`, `slack`, `springcm`, `zendesk`, `zoom`.
                    type: string
                  callbacks:
                    description: |-
                      (List of String) URLs that Auth0 may call back to after a user authenticates for the client. Make sure to specify the protocol (https://) otherwise the callback may fail in some cases. With the exception of custom URI schemes for native clients, all callbacks should use protocol https://.
                      URLs that Auth0 may call back to after a user authenticates for the client. Make sure to specify the protocol (https://) otherwise the callback may fail in some cases. With the exception of custom URI schemes for native clients, all callbacks should use protocol https://.
                    items:
                      type: string
                    type: array
                  clientAliases:
                    description: |-
                      (List of String) List of audiences/realms for SAML protocol. Used by the wsfed addon.
                      List of audiences/realms for SAML protocol. Used by the wsfed addon.
                    items:
                      type: string
                    type: array
                  clientMetadata:
                    additionalProperties:
                      type: string
                    description: 'Metadata associated with the client, in the form
                      of an object with string values (max 255 chars). Maximum of
                      10 metadata properties allowed. Field names (max 255 chars)
                      are alphanumeric and may only include the following special
                      characters: `:,-+=_*?"/\()<>@ [Tab] [Space]`.'
                    type: object
                    x-kubernetes-map-type: granular
                  complianceLevel:
                    description: |-
                      (String) Defines the compliance level for this client, which may restrict it's capabilities. Can be one of none, fapi1_adv_pkj_par, fapi1_adv_mtls_par.
                      Defines the compliance level for this client, which may restrict it's capabilities. Can be one of `none`, `fapi1_adv_pkj_par`, `fapi1_adv_mtls_par`.
                    type: string
                  crossOriginAuth:
                    description: |-
                      origin authentication requests (true) or it is not allowed to make such requests (false).
                      Whether this client can be used to make cross-origin authentication requests (`true`) or it is not allowed to make such requests (`false`).
                    type: boolean
                  crossOriginLoc:
                    description: |-
                      origin verification takes place for the cross-origin auth flow when performing authentication in your own domain instead of Auth0 Universal Login page.
                      URL of the location in your site where the cross-origin verification takes place for the cross-origin auth flow when performing authentication in your own domain instead of Auth0 Universal Login page.
                    type: string
                  customLoginPage:
                    description: |-
                      (String) The content (HTML, CSS, JS) of the custom login page.
                      The content (HTML, CSS, JS) of the custom login page.
                    type: string
                  customLoginPageOn:
                    description: |-
                      (Boolean) Indicates whether a custom login page is to be used.
                      Indicates whether a custom login page is to be used.
                    type: boolean
                  defaultOrganization:
                    description: |-
                      (Block List, Max: 1) Configure and associate an organization with the Client (see below for nested schema)
                      Configure and associate an organization with the Client
                    items:
                      properties:
                        disable:
                          description: |-
                            (Boolean) If set, the default_organization will be removed.
                            If set, the `default_organization` will be removed.
                          type: boolean
                        flows:
                          description: |-
                            (List of String) Definition of the flow that needs to be configured. Eg. client_credentials
                            Definition of the flow that needs to be configured. Eg. client_credentials
                          items:
                            type: string
                          type: array
                        organizationId:
                          description: |-
                            (String) The unique identifier of the organization
                            The unique identifier of the organization
                          type: string
                      type: object
                    type: array
                  description:
                    description: |-
                      (String) Description of the purpose of the client.
                      Description of the purpose of the client.
                    type: string
                  encryptionKey:
                    additionalProperties:
                      type: string
                    description: |-
                      Fed responses with this client.
                      Encryption used for WS-Fed responses with this client.
                    type: object
                    x-kubernetes-map-type: granular
                  formTemplate:
                    description: |-
                      Federation.
                      HTML form template to be used for WS-Federation.
                    type: string
                  grantTypes:
                    description: |-
                      (List of String) Types of grants that this client is authorized to use.
                      Types of grants that this client is authorized to use.
                    items:
                      type: string
                    type: array
                  initiateLoginUri:
                    description: |-
                      (String) Initiate login URI. Must be HTTPS or an empty string.
                      Initiate login URI. Must be HTTPS or an empty string.
                    type: string
                  isFirstParty:
                    description: |-
                      party client.Defaults to true from the API
                      Indicates whether this client is a first-party client.Defaults to true from the API
                    type: boolean
                  isTokenEndpointIpHeaderTrusted:
                    description: |-
                      (Boolean) Indicates whether the token endpoint IP header is trusted. Requires the authentication method to be set to client_secret_post or client_secret_basic. Setting this property when creating the resource, will default the authentication method to client_secret_post. To change the authentication method to client_secret_basic use the auth0_client_credentials resource.
                      Indicates whether the token endpoint IP header is trusted. Requires the authentication method to be set to `client_secret_post` or `client_secret_basic`. Setting this property when creating the resource, will default the authentication method to `client_secret_post`. To change the authentication method to `client_secret_basic` use the `auth0_client_credentials` resource.
                    type: boolean
                  jwtConfiguration:
                    description: |-
                      (Block List, Max: 1) Configuration settings for the JWTs issued for this client. (see below for nested schema)
                      Configuration settings for the JWTs issued for this client.
                    items:
                      properties:
                        alg:
                          description: |-
                            (String) Algorithm used to sign JWTs. Can be one of HS256, RS256, PS256.
                            Algorithm used to sign JWTs. Can be one of `HS256`, `RS256`, `PS256`.
                          type: string
                        lifetimeInSeconds:
                          description: |-
                            (Number) AWS token lifetime in seconds.
                            Number of seconds during which the JWT will be valid.
                          type: number
                        scopes:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Permissions (scopes) included in JWTs.
                            Permissions (scopes) included in JWTs.
                          type: object
                          x-kubernetes-map-type: granular
                        secretEncoded:
                          description: |-
                            encoded.
                            Indicates whether the client secret is Base64-encoded.
                          type: boolean
                      type: object
                    type: array
                  logoUri:
                    description: |-
                      (String) URL of the logo for the client. Recommended size is 150px x 150px. If none is set, the default badge for the application type will be shown.
                      URL of the logo for the client. Recommended size is 150px x 150px. If none is set, the default badge for the application type will be shown.
                    type: string
                  mobile:
                    description: |-
                      (Block List, Max: 1) Additional configuration for native mobile apps. (see below for nested schema)
                      Additional configuration for native mobile apps.
                    items:
                      properties:
                        android:
                          description: |-
                            (Block List, Max: 1) Configuration settings for Android native apps. (see below for nested schema)
                            Configuration settings for Android native apps.
                          items:
                            properties:
                              appPackageName:
                                description: (String)
                                type: string
                              sha256CertFingerprints:
                                description: (List of String)
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        ios:
                          description: |-
                            (Block List, Max: 1) Configuration settings for i0S native apps. (see below for nested schema)
                            Configuration settings for i0S native apps.
                          items:
                            properties:
                              appBundleIdentifier:
                                description: (String)
                                type: string
                              teamId:
                                description: (String)
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  nativeSocialLogin:
                    description: |-
                      (Block List, Max: 1) Configuration settings to toggle native social login for mobile native applications. Once this is set it must stay set, with both resources set to false in order to change the app_type. (see below for nested schema)
                      Configuration settings to toggle native social login for mobile native applications. Once this is set it must stay set, with both resources set to `false` in order to change the `app_type`.
                    items:
                      properties:
                        apple:
                          description: '(Block List, Max: 1) (see below for nested
                            schema)'
                          items:
                            properties:
                              enabled:
                                description: (Boolean)
                                type: boolean
                            type: object
                          type: array
                        facebook:
                          description: '(Block List, Max: 1) (see below for nested
                            schema)'
                          items:
                            properties:
                              enabled:
                                description: (Boolean)
                                type: boolean
                            type: object
                          type: array
                        google:
                          description: '(Block List, Max: 1) (see below for nested
                            schema)'
                          items:
                            properties:
                              enabled:
                                description: (Boolean)
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  oidcBackchannelLogoutUrls:
                    description: |-
                      (Set of String, Deprecated) Set of URLs that are valid to call back from Auth0 for OIDC backchannel logout. Currently only one URL is allowed.
                      Set of URLs that are valid to call back from Auth0 for OIDC backchannel logout. Currently only one URL is allowed.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  oidcConformant:
                    description: |-
                      (Boolean) Indicates whether this client will conform to strict OIDC specifications.
                      Indicates whether this client will conform to strict OIDC specifications.
                    type: boolean
                  oidcLogout:
                    description: |-
                      (Block List, Max: 1) Configure OIDC logout for the Client (see below for nested schema)
                      Configure OIDC logout for the Client
                    items:
                      properties:
                        backchannelLogoutInitiators:
                          description: |-
                            (Block List, Max: 1) Configure OIDC logout initiators for the Client (see below for nested schema)
                            Configure OIDC logout initiators for the Client
                          items:
                            properties:
                              mode:
                                description: |-
                                  (String) Determines the configuration method for enabling initiators. custom enables only the initiators listed in the backchannel_logout_selected_initiators set, all enables all current and future initiators.
                                  Determines the configuration method for enabling initiators. `custom` enables only the initiators listed in the backchannel_logout_selected_initiators set, `all` enables all current and future initiators.
                                type: string
                              selectedInitiators:
                                description: |-
                                  (Set of String) Contains the list of initiators to be enabled for the given client.
                                  Contains the list of initiators to be enabled for the given client.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        backchannelLogoutUrls:
                          description: |-
                            (Set of String) Set of URLs that are valid to call back from Auth0 for OIDC backchannel logout. Currently only one URL is allowed.
                            Set of URLs that are valid to call back from Auth0 for OIDC backchannel logout. Currently only one URL is allowed.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  organizationRequireBehavior:
                    description: |-
                      (String) Defines how to proceed during an authentication transaction when organization_usage = "require". Can be no_prompt (default), pre_login_prompt or  post_login_prompt.
                      Defines how to proceed during an authentication transaction when `organization_usage = "require"`. Can be `no_prompt` (default), `pre_login_prompt` or  `post_login_prompt`.
                    type: string
                  organizationUsage:
                    description: |-
                      (String) Defines how to proceed during an authentication transaction with regards to an organization. Can be deny (default), allow or require.
                      Defines how to proceed during an authentication transaction with regards to an organization. Can be `deny` (default), `allow` or `require`.
                    type: string
                  refreshToken:
                    description: |-
                      (Block List, Max: 1) Configuration settings for the refresh tokens issued for this client. (see below for nested schema)
                      Configuration settings for the refresh tokens issued for this client.
                    items:
                      properties:
                        expirationType:
                          description: |-
                            expiring. Whether a refresh token will expire based on an absolute lifetime, after which the token can no longer be used. If rotation is rotating, this must be set to expiring.
                            Options include `expiring`, `non-expiring`. Whether a refresh token will expire based on an absolute lifetime, after which the token can no longer be used. If rotation is `rotating`, this must be set to `expiring`.
                          type: string
                        idleTokenLifetime:
                          description: |-
                            (Number) The time in seconds after which inactive refresh tokens will expire.
                            The time in seconds after which inactive refresh tokens will expire.
                          type: number
                        infiniteIdleTokenLifetime:
                          description: |-
                            (Boolean) Whether inactive refresh tokens should remain valid indefinitely.
                            Whether inactive refresh tokens should remain valid indefinitely.
                          type: boolean
                        infiniteTokenLifetime:
                          description: |-
                            (Boolean) Whether refresh tokens should remain valid indefinitely. If false, token_lifetime should also be set.
                            Whether refresh tokens should remain valid indefinitely. If false, `token_lifetime` should also be set.
                          type: boolean
                        leeway:
                          description: |-
                            (Number) The amount of time in seconds in which a refresh token may be reused without triggering reuse detection.
                            The amount of time in seconds in which a refresh token may be reused without triggering reuse detection.
                          type: number
                        policies:
                          description: |-
                            resource refresh token exchange (MRRT), defining how refresh tokens can be used across different resource servers (see below for nested schema)
                            A collection of policies governing multi-resource refresh token exchange (MRRT), defining how refresh tokens can be used across different resource servers
                          items:
                            properties:
                              audience:
                                description: |-
                                  (String) Audience of the SAML Assertion. Default will be the Issuer on SAMLRequest.
                                  The identifier of the resource server to which the Multi Resource Refresh Token Policy applies
                                type: string
                              scope:
                                description: |-
                                  (String) Requested scope for SAP APIs.
                                  The resource server permissions granted under the Multi Resource Refresh Token Policy, defining the context in which an access token can be used
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        rotationType:
                          description: |-
                            rotating. When rotating, exchanging a refresh token will cause a new refresh token to be issued and the existing token will be invalidated. This allows for automatic detection of token reuse if the token is leaked.
                            Options include `rotating`, `non-rotating`. When `rotating`, exchanging a refresh token will cause a new refresh token to be issued and the existing token will be invalidated. This allows for automatic detection of token reuse if the token is leaked.
                          type: string
                        tokenLifetime:
                          description: |-
                            (Number) The absolute lifetime of a refresh token in seconds.
                            The absolute lifetime of a refresh token in seconds.
                          type: number
                      type: object
                    type: array
                  requireProofOfPossession:
                    description: |-
                      of-Possession mandatory for this client.
                      Makes the use of Proof-of-Possession mandatory for this client.
                    type: boolean
                  requirePushedAuthorizationRequests:
                    description: |-
                      (Boolean) Makes the use of Pushed Authorization Requests mandatory for this client. This feature currently needs to be enabled on the tenant in order to make use of it.
                      Makes the use of Pushed Authorization Requests mandatory for this client. This feature currently needs to be enabled on the tenant in order to make use of it.
                    type: boolean
                  sessionTransfer:
                    description: '(Block List, Max: 1) (see below for nested schema)'
                    items:
                      properties:
                        allowRefreshToken:
                          description: |-
                            (Boolean) Indicates whether the application is allowed to use a refresh token when using a session_transfer_token session.
                            Indicates whether the application is allowed to use a refresh token when using a session_transfer_token session.
                          type: boolean
                        allowedAuthenticationMethods:
                          description: (Set of String)
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        canCreateSessionTransferToken:
                          description: |-
                            (Boolean) Indicates whether the application(Native app) can use the Token Exchange endpoint to create a session_transfer_token
                            Indicates whether the application(Native app) can use the Token Exchange endpoint to create a session_transfer_token
                          type: boolean
                        enforceDeviceBinding:
                          description: |-
                            (String) Configures the level of device binding enforced when a session_transfer_token is consumed. Can be one of ip, asn or none.
                            Configures the level of device binding enforced when a session_transfer_token is consumed. Can be one of `ip`, `asn` or `none`.
                          type: string
                      type: object
                    type: array
                  sso:
                    description: |-
                      On (true) or whether the identity provider will (false).
                      Applies only to SSO clients and determines whether Auth0 will handle Single Sign-On (true) or whether the identity provider will (false).
                    type: boolean
                  ssoDisabled:
                    description: |-
                      (Boolean) Indicates whether or not SSO is disabled.
                      Indicates whether or not SSO is disabled.
                    type: boolean
                  tokenExchange:
                    description: |-
                      (Block List, Max: 1) Allows configuration for token exchange (see below for nested schema)
                      Allows configuration for token exchange
                    items:
                      properties:
                        allowAnyProfileOfType:
                          description: |-
                            (List of String) List of allowed profile types for token exchange
                            List of allowed profile types for token exchange
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  tokenQuota:
                    description: |-
                      (Block List, Max: 1) The token quota configuration. (see below for nested schema)
                      The token quota configuration.
                    items:
                      properties:
                        clientCredentials:
                          description: |-
                            (Block List, Min: 1, Max: 1) The token quota configuration for client credentials. (see below for nested schema)
                            The token quota configuration for client credentials.
                          items:
                            properties:
                              enforce:
                                description: |-
                                  (Boolean) If enabled, the quota will be enforced and requests in excess of the quota will fail. If disabled, the quota will not be enforced, but notifications for requests exceeding the quota will be available in logs.
                                  If enabled, the quota will be enforced and requests in excess of the quota will fail. If disabled, the quota will not be enforced, but notifications for requests exceeding the quota will be available in logs.
                                type: boolean
                              perDay:
                                description: |-
                                  (Number) Maximum number of issued tokens per day
                                  Maximum number of issued tokens per day
                                type: number
                              perHour:
                                description: |-
                                  (Number) Maximum number of issued tokens per hour
                                  Maximum number of issued tokens per hour
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  webOrigins:
                    description: |-
                      (List of String) URLs that represent valid web origins for use with web message response mode.
                      URLs that represent valid web origins for use with web message response mode.
                    items:
                      type: string
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  addons:
                    description: |-
                      (Block List, Max: 1) Addons enabled for this client and their associated configurations. (see below for nested schema)
                      Addons enabled for this client and their associated configurations.
                    items:
                      properties:
                        aws:
                          description: |-
                            (Block List, Max: 1) AWS Addon configuration. (see below for nested schema)
                            AWS Addon configuration.
                          items:
                            properties:
                              lifetimeInSeconds:
                                description: |-
                                  (Number) AWS token lifetime in seconds.
                                  AWS token lifetime in seconds.
                                type: number
                              principal:
                                description: |-
                                  provider/idpname.
                                  AWS principal ARN, for example `arn:aws:iam::010616021751:saml-provider/idpname`.
                                type: string
                              role:
                                description: |-
                                  (String) AWS role ARN, for example arn:aws:iam::010616021751:role/foo.
                                  AWS role ARN, for example `arn:aws:iam::010616021751:role/foo`.
                                type: string
                            type: object
                          type: array
                        azureBlob:
                          description: |-
                            (Block List, Max: 1) Azure Blob Storage Addon configuration. (see below for nested schema)
                            Azure Blob Storage Addon configuration.
                          items:
                            properties:
                              accountName:
                                description: |-
                                  org.blob.core.windows.net would be the account name acme-org.
                                  Your Azure storage account name. Usually first segment in your Azure storage URL, for example `https://acme-org.blob.core.windows.net` would be the account name `acme-org`.
                                type: string
                              blobDelete:
                                description: |-
                                  (Boolean) Indicates if the issued token has permission to delete the blob.
                                  Indicates if the issued token has permission to delete the blob.
                                type: boolean
                              blobName:
                                description: |-
                                  blob. If blank the computed SAS will apply to the entire storage container.
                                  Entity to request a token for, such as `my-blob`. If blank the computed SAS will apply to the entire storage container.
                                type: string
                              blobRead:
                                description: |-
                                  (Boolean) Indicates if the issued token has permission to read the content, properties, metadata and block list. Use the blob as the source of a copy operation.
                                  Indicates if the issued token has permission to read the content, properties, metadata and block list. Use the blob as the source of a copy operation.
                                type: boolean
                              blobWrite:
                                description: |-
                                  (Boolean) Indicates if the issued token has permission to create or write content, properties, metadata, or block list. Snapshot or lease the blob. Resize the blob (page blob only). Use the blob as the destination of a copy operation within the same account.
                                  Indicates if the issued token has permission to create or write content, properties, metadata, or block list. Snapshot or lease the blob. Resize the blob (page blob only). Use the blob as the destination of a copy operation within the same account.
                                type: boolean
                              containerDelete:
                                description: |-
                                  (Boolean) Indicates if issued token has permission to delete any blob in the container.
                                  Indicates if issued token has permission to delete any blob in the container.
                                type: boolean
                              containerList:
                                description: |-
                                  (Boolean) Indicates if the issued token has permission to list blobs in the container.
                                  Indicates if the issued token has permission to list blobs in the container.
                                type: boolean
                              containerName:
                                description: |-
                                  container.
                                  Container to request a token for, such as `my-container`.
                                type: string
                              containerRead:
                                description: |-
                                  (Boolean) Indicates if the issued token has permission to read the content, properties, metadata or block list of any blob in the container. Use any blob in the container as the source of a copy operation.
                                  Indicates if the issued token has permission to read the content, properties, metadata or block list of any blob in the container. Use any blob in the container as the source of a copy operation.
                                type: boolean
                              containerWrite:
                                description: |-
                                  (Boolean) Indicates that for any blob in the container if the issued token has permission to create or write content, properties, metadata, or block list. Snapshot or lease the blob. Resize the blob (page blob only). Use the blob as the destination of a copy operation within the same account.
                                  Indicates that for any blob in the container if the issued token has permission to create or write content, properties, metadata, or block list. Snapshot or lease the blob. Resize the blob (page blob only). Use the blob as the destination of a copy operation within the same account.
                                type: boolean
                              expiration:
                                description: |-
                                  (Number) Expiration in minutes for the generated token (default of 5 minutes).
                                  Expiration in minutes for the generated token (default of 5 minutes).
                                type: number
                              signedIdentifier:
                                description: |-
                                  (String) Shared access policy identifier defined in your storage account resource.
                                  Shared access policy identifier defined in your storage account resource.
                                type: string
                              storageAccessKeySecretRef:
                                description: |-
                                  (String, Sensitive) Access key associated with this storage account.
                                  Access key associated with this storage account.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          type: array
                        azureSb:
                          description: |-
                            (Block List, Max: 1) Azure Storage Bus Addon configuration. (see below for nested schema)
                            Azure Storage Bus Addon configuration.
                          items:
                            properties:
                              entityPath:
                                description: |-
                                  queue.
                                  Entity you want to request a token for, such as `my-queue`.
                                type: string
                              expiration:
                                description: |-
                                  (Number) Expiration in minutes for the generated token (default of 5 minutes).
                                  Optional expiration in minutes for the generated token. Defaults to 5 minutes.
                                type: number
                              namespace:
                                description: |-
                                  org.servicebus.windows.net would be acme-org).
                                  Your Azure Service Bus namespace. Usually the first segment of your Service Bus URL (for example `https://acme-org.servicebus.windows.net` would be `acme-org`).
                                type: string
                              sasKeyName:
                                description: |-
                                  (String) Your shared access policy name defined in your Service Bus entity.
                                  Your shared access policy name defined in your Service Bus entity.
                                type: string
                              sasKeySecretRef:
                                description: |-
                                  (String, Sensitive) Primary Key associated with your shared access policy.
                                  Primary Key associated with your shared access policy.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          type: array
                        box:
                          description: |-
                            (Block List, Max: 1) Box SSO indicator (no configuration settings needed for Box SSO). (see below for nested schema)
                            Box SSO indicator (no configuration settings needed for Box SSO).
                          items:
                            type: object
                          type: array
                        cloudbees:
                          description: |-
                            (Block List, Max: 1) CloudBees SSO indicator (no configuration settings needed for CloudBees SSO). (see below for nested schema)
                            CloudBees SSO indicator (no configuration settings needed for CloudBees SSO).
                          items:
                            type: object
                          type: array
                        concur:
                          description: |-
                            (Block List, Max: 1) Concur SSO indicator (no configuration settings needed for Concur SSO). (see below for nested schema)
                            Concur SSO indicator (no configuration settings needed for Concur SSO).
                          items:
                            type: object
                          type: array
                        dropbox:
                          description: |-
                            (Block List, Max: 1) Dropbox SSO indicator (no configuration settings needed for Dropbox SSO). (see below for nested schema)
                            Dropbox SSO indicator (no configuration settings needed for Dropbox SSO).
                          items:
                            type: object
                          type: array
                        echosign:
                          description: |-
                            (Block List, Max: 1) Adobe EchoSign SSO configuration. (see below for nested schema)
                            Adobe EchoSign SSO configuration.
                          items:
                            properties:
                              domain:
                                description: |-
                                  org.echosign.com would be acme-org.
                                  Your custom domain found in your EchoSign URL, for example `https://acme-org.echosign.com` would be `acme-org`.
                                type: string
                            type: object
                          type: array
                        egnyte:
                          description: |-
                            (Block List, Max: 1) Egnyte SSO configuration. (see below for nested schema)
                            Egnyte SSO configuration.
                          items:
                            properties:
                              domain:
                                description: |-
                                  org.echosign.com would be acme-org.
                                  Your custom domain found in your Egnyte URL, for example `https://acme-org.echosign.com` would be `acme-org`.
                                type: string
                            type: object
                          type: array
                        firebase:
                          description: |-
                            (Block List, Max: 1) Google Firebase addon configuration. (see below for nested schema)
                            Google Firebase addon configuration.
                          items:
                            properties:
                              clientEmail:
                                description: |-
                                  (String) ID of the Service Account you have created (shown as client_email in the generated JSON file, SDK v3+ tokens only).
                                  ID of the Service Account you have created (shown as `client_email` in the generated JSON file, SDK v3+ tokens only).
                                type: string
                              lifetimeInSeconds:
                                description: |-
                                  (Number) AWS token lifetime in seconds.
                                  Optional expiration in seconds for the generated token. Defaults to 3600 seconds (SDK v3+ tokens only).
                                type: number
                              privateKeyIdSecretRef:
                                description: |-
                                  (String, Sensitive) Optional ID of the private key to obtain the kid header claim from the issued token (SDK v3+ tokens only).
                                  Optional ID of the private key to obtain the `kid` header claim from the issued token (SDK v3+ tokens only).
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              privateKeySecretRef:
                                description: |-
                                  (String, Sensitive) Private Key for signing the token (SDK v3+ tokens only).
                                  Private Key for signing the token (SDK v3+ tokens only).
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              secretSecretRef:
                                description: |-
                                  (String, Sensitive) Google Firebase Secret. (SDK v2 only).
                                  Google Firebase Secret. (SDK v2 only).
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          type: array
                        layer:
                          description: |-
                            (Block List, Max: 1) Layer addon configuration. (see below for nested schema)
                            Layer addon configuration.
                          items:
                            properties:
                              expiration:
                                description: |-
                                  (Number) Expiration in minutes for the generated token (default of 5 minutes).
                                  Optional expiration in minutes for the generated token. Defaults to 5 minutes.
                                type: number
                              keyIdSecretRef:
                                description: |-
                                  (String, Sensitive) Authentication Key identifier used to sign the Layer token.
                                  Authentication Key identifier used to sign the Layer token.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              principal:
                                description: |-
                                  provider/idpname.
                                  Name of the property used as the unique user ID in Layer. If not specified `user_id` is used.
                                type: string
                              privateKeySecretRef:
                                description: |-
                                  (String, Sensitive) Private Key for signing the token (SDK v3+ tokens only).
                                  Private key for signing the Layer token.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              providerId:
                                description: |-
                                  (String) Provider ID of your Layer account.
                                  Provider ID of your Layer account.
                                type: string
                            required:
                            - keyIdSecretRef
                            - privateKeySecretRef
                            type: object
                          type: array
                        mscrm:
                          description: |-
                            (Block List, Max: 1) Microsoft Dynamics CRM SSO configuration. (see below for nested schema)
                            Microsoft Dynamics CRM SSO configuration.
                          items:
                            properties:
                              url:
                                description: |-
                                  (String) Microsoft Dynamics CRM application URL.
                                  Microsoft Dynamics CRM application URL.
                                type: string
                            type: object
                          type: array
                        newrelic:
                          description: |-
                            (Block List, Max: 1) New Relic SSO configuration. (see below for nested schema)
                            New Relic SSO configuration.
                          items:
                            properties:
                              account:
                                description: |-
                                  (String) Your New Relic Account ID found in your New Relic URL after the /accounts/ path, for example https://rpm.newrelic.com/accounts/123456/query would be 123456.
                                  Your New Relic Account ID found in your New Relic URL after the `/accounts/` path, for example `https://rpm.newrelic.com/accounts/123456/query` would be `123456`.
                                type: string
                            type: object
                          type: array
                        office365:
                          description: |-
                            (Block List, Max: 1) Microsoft Office 365 SSO configuration. (see below for nested schema)
                            Microsoft Office 365 SSO configuration.
                          items:
                            properties:
                              connection:
                                description: |-
                                  configured Office 365 tenant.
                                  Optional Auth0 database connection for testing an already-configured Office 365 tenant.
                                type: string
                              domain:
                                description: |-
                                  org.echosign.com would be acme-org.
                                  Your Office 365 domain name, for example `acme-org.com`.
                                type: string
                            type: object
                          type: array
                        rms:
                          description: |-
                            (Block List, Max: 1) Active Directory Rights Management Service SSO configuration. (see below for nested schema)
                            Active Directory Rights Management Service SSO configuration.
                          items:
                            properties:
                              url:
                                description: |-
                                  (String) Microsoft Dynamics CRM application URL.
                                  URL of your Rights Management Server. It can be internal or external, but users will have to be able to reach it.
                                type: string
                            type: object
                          type: array
                        salesforce:
                          description: |-
                            (Block List, Max: 1) Salesforce SSO configuration. (see below for nested schema)
                            Salesforce SSO configuration.
                          items:
                            properties:
                              entityId:
                                description: |-
                                  org.com.
                                  Arbitrary logical URL that identifies the Saleforce resource, for example `https://acme-org.com`.
                                type: string
                            type: object
                          type: array
                        salesforceApi:
                          description: |-
                            (Block List, Max: 1) Salesforce API addon configuration. (see below for nested schema)
                            Salesforce API addon configuration.
                          items:
                            properties:
                              clientIdSecretRef:
                                description: |-
                                  (String) The ID of the client.
                                  Consumer Key assigned by Salesforce to the Connected App.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              communityName:
                                description: |-
                                  (String) Community name.
                                  Community name.
                                type: string
                              communityUrlSection:
                                description: |-
                                  (String) Community URL section.
                                  Community URL section.
                                type: string
                              principalSecretRef:
                                description: |-
                                  provider/idpname.
                                  Name of the property in the user object that maps to a Salesforce username, for example `email`.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          type: array
                        salesforceSandboxApi:
                          description: |-
                            (Block List, Max: 1) Salesforce Sandbox addon configuration. (see below for nested schema)
                            Salesforce Sandbox addon configuration.
                          items:
                            properties:
                              clientIdSecretRef:
                                description: |-
                                  (String) The ID of the client.
                                  Consumer Key assigned by Salesforce to the Connected App.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              communityName:
                                description: |-
                                  (String) Community name.
                                  Community name.
                                type: string
                              communityUrlSection:
                                description: |-
                                  (String) Community URL section.
                                  Community URL section.
                                type: string
                              principalSecretRef:
                                description: |-
                                  provider/idpname.
                                  Name of the property in the user object that maps to a Salesforce username, for example `email`.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          type: array
                        samlp:
                          description: |-
                            on. (see below for nested schema)
                            Configuration settings for a SAML add-on.
                          items:
                            properties:
                              audience:
                                description: |-
                                  (String) Audience of the SAML Assertion. Default will be the Issuer on SAMLRequest.
                                  Audience of the SAML Assertion. Default will be the Issuer on SAMLRequest.
                                type: string
                              authnContextClassRef:
                                description: |-
                                  (String) Class reference of the authentication context.
                                  Class reference of the authentication context.
                                type: string
                              binding:
                                description: |-
                                  (String) Protocol binding used for SAML logout responses.
                                  Protocol binding used for SAML logout responses.
                                type: string
                              createUpnClaim:
                                description: |-
                                  (Boolean) Indicates whether a UPN claim should be created. Defaults to true.
                                  Indicates whether a UPN claim should be created. Defaults to `true`.
                                type: boolean
                              destination:
                                description: |-
                                  (String) Destination of the SAML Response. If not specified, it will be AssertionConsumerUrl of SAMLRequest or callback URL if there was no SAMLRequest.
                                  Destination of the SAML Response. If not specified, it will be `AssertionConsumerUrl` of SAMLRequest or callback URL if there was no SAMLRequest.
                                type: string
                              digestAlgorithm:
                                description: |-
                                  (String) Algorithm used to calculate the digest of the SAML Assertion or response. Options include sha1 and sha256. Defaults to sha1.
                                  Algorithm used to calculate the digest of the SAML Assertion or response. Options include `sha1` and `sha256`. Defaults to `sha1`.
                                type: string
                              flexibleMappings:
                                description: |-
                                  (String) This is a supporting attribute to mappings field.Please note this is an experimental field. It should only be used when needed to send a map with keys as slices.
                                  This is a supporting attribute to `mappings` field.Please note this is an experimental field. It should only be used when needed to send a map with keys as slices.
                                type: string
                              includeAttributeNameFormat:
                                description: |-
                                  (Boolean) Indicates whether or not we should infer the NameFormat based on the attribute name. If set to false, the attribute NameFormat is not set in the assertion. Defaults to true.
                                  Indicates whether or not we should infer the NameFormat based on the attribute name. If set to `false`, the attribute NameFormat is not set in the assertion. Defaults to `true`.
                                type: boolean
                              issuer:
                                description: |-
                                  (String) Issuer of the SAML Assertion.
                                  Issuer of the SAML Assertion.
                                type: string
                              lifetimeInSeconds:
                                description: |-
                                  (Number) AWS token lifetime in seconds.
                                  Number of seconds during which the token is valid. Defaults to `3600` seconds.
                                type: number
                              logout:
                                description: |-
                                  (Block List, Max: 1) Configuration settings for logout. (see below for nested schema)
                                  Configuration settings for logout.
                                items:
                                  properties:
                                    callback:
                                      description: |-
                                        (String) The service provider (client application)'s Single Logout Service URL, where Auth0 will send logout requests and responses.
                                        The service provider (client application)'s Single Logout Service URL, where Auth0 will send logout requests and responses.
                                      type: string
                                    sloEnabled:
                                      description: |-
                                        (Boolean) Controls whether Auth0 should notify service providers of session termination.
                                        Controls whether Auth0 should notify service providers of session termination.
                                      type: boolean
                                  type: object
                                type: array
                              mapIdentities:
                                description: |-
                                  (Boolean) Indicates whether or not to add additional identity information in the token, such as the provider used and the access_token, if available. Defaults to true.
                                  Indicates whether or not to add additional identity information in the token, such as the provider used and the `access_token`, if available. Defaults to `true`.
                                type: boolean
                              mapUnknownClaimsAsIs:
                                description: |-
                                  (Boolean) Indicates whether to add a prefix of http://schema.auth0.com to any claims that are not mapped to the common profile when passed through in the output assertion. Defaults to false.
                                  Indicates whether to add a prefix of `http://schema.auth0.com` to any claims that are not mapped to the common profile when passed through in the output assertion. Defaults to `false`.
                                type: boolean
                              mappings:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Mappings between the Auth0 user profile property name (name) and the output attributes on the SAML attribute in the assertion (value).
                                  Mappings between the Auth0 user profile property name (`name`) and the output attributes on the SAML attribute in the assertion (`value`).
                                type: object
                                x-kubernetes-map-type: granular
                              nameIdentifierFormat:
                                description: |-
                                  format:unspecified.
                                  Format of the name identifier. Defaults to `urn:oasis:names:tc:SAML:1.1:nameid-format:unspecified`.
                                type: string
                              nameIdentifierProbes:
                                description: |-
                                  (List of String) Attributes that can be used for Subject/NameID. Auth0 will try each of the attributes of this array in order and use the first value it finds.
                                  Attributes that can be used for Subject/NameID. Auth0 will try each of the attributes of this array in order and use the first value it finds.
                                items:
                                  type: string
                                type: array
                              passthroughClaimsWithNoMapping:
                                description: |-
                                  (Boolean) Indicates whether or not to passthrough claims that are not mapped to the common profile in the output assertion. Defaults to true.
                                  Indicates whether or not to passthrough claims that are not mapped to the common profile in the output assertion. Defaults to `true`.
                                type: boolean
                              recipient:
                                description: |-
                                  (String) Recipient of the SAML Assertion (SubjectConfirmationData). Default is AssertionConsumerUrl on SAMLRequest or callback URL if no SAMLRequest was sent.
                                  Recipient of the SAML Assertion (SubjectConfirmationData). Default is `AssertionConsumerUrl` on SAMLRequest or callback URL if no SAMLRequest was sent.
                                type: string
                              signResponse:
                                description: |-
                                  (Boolean) Indicates whether or not the SAML Response should be signed instead of the SAML Assertion.
                                  Indicates whether or not the SAML Response should be signed instead of the SAML Assertion.
                                type: boolean
                              signatureAlgorithm:
                                description: |-
                                  sha1 and rsa-sha256. Defaults to rsa-sha1.
                                  Algorithm used to sign the SAML Assertion or response. Options include `rsa-sha1` and `rsa-sha256`. Defaults to `rsa-sha1`.
                                type: string
                              signingCert:
                                type: string
                              typedAttributes:
                                description: |-
                                  (Boolean) Indicates whether or not we should infer the xs:type of the element. Types include xs:string, xs:boolean, xs:double, and xs:anyType. When set to false, all xs:type are xs:anyType. Defaults to true.
                                  Indicates whether or not we should infer the `xs:type` of the element. Types include `xs:string`, `xs:boolean`, `xs:double`, and `xs:anyType`. When set to `false`, all `xs:type` are `xs:anyType`. Defaults to `true`.
                                type: boolean
                            type: object
                          type: array
                        sapApi:
                          description: |-
                            (Block List, Max: 1) SAP API addon configuration. (see below for nested schema)
                            SAP API addon configuration.
                          items:
                            properties:
                              clientId:
                                description: |-
                                  (String) The ID of the client.
                                  If activated in the OAuth 2.0 client configuration (transaction `SOAUTH2) the SAML attribute `client_id` must be set and equal the `client_id` form parameter of the access token request.
                                type: string
                              nameIdentifierFormat:
                                description: |-
                                  format:unspecified.
                                  NameID element of the Subject which can be used to express the user's identity. Defaults to `urn:oasis:names:tc:SAML:1.1:nameid-format:unspecified`.
                                type: string
                              scope:
                                description: |-
                                  (String) Requested scope for SAP APIs.
                                  Requested scope for SAP APIs.
                                type: string
                              servicePasswordSecretRef:
                                description: |-
                                  (String, Sensitive) Service account password to use to authenticate API calls to the token endpoint.
                                  Service account password to use to authenticate API calls to the token endpoint.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              tokenEndpointUrl:
                                description: |-
                                  (String) The OAuth2 token endpoint URL of your SAP OData server.
                                  The OAuth2 token endpoint URL of your SAP OData server.
                                type: string
                              usernameAttribute:
                                description: |-
                                  (String) Name of the property in the user object that maps to a SAP username, for example email.
                                  Name of the property in the user object that maps to a SAP username, for example `email`.
                                type: string
                            type: object
                          type: array
                        sentry:
                          description: |-
                            (Block List, Max: 1) Sentry SSO configuration. (see below for nested schema)
                            Sentry SSO configuration.
                          items:
                            properties:
                              baseUrl:
                                description: |-
                                  (String) URL prefix only if running Sentry Community Edition, otherwise leave empty.
                                  URL prefix only if running Sentry Community Edition, otherwise leave empty.
                                type: string
                              orgSlug:
                                description: |-
                                  org/ would be acme-org.
                                  Generated slug for your Sentry organization. Found in your Sentry URL, for example `https://sentry.acme.com/acme-org/` would be `acme-org`.
                                type: string
                            type: object
                          type: array
                        sharepoint:
                          description: |-
                            (Block List, Max: 1) SharePoint SSO configuration. (see below for nested schema)
                            SharePoint SSO configuration.
                          items:
                            properties:
                              externalUrl:
                                description: |-
                                  (List of String) External SharePoint application URLs if exposed to the Internet.
                                  External SharePoint application URLs if exposed to the Internet.
                                items:
                                  type: string
                                type: array
                              url:
                                description: |-
                                  (String) Microsoft Dynamics CRM application URL.
                                  Internal SharePoint application URL.
                                type: string
                            type: object
                          type: array
                        slack:
                          description: |-
                            org.slack.com would be acme-org. (see below for nested schema)
                            Slack team or workspace name usually first segment in your Slack URL, for example `https://acme-org.slack.com` would be `acme-org`.
                          items:
                            properties:
                              team:
                                description: |-
                                  (String) Slack team name.
                                  Slack team name.
                                type: string
                            type: object
                          type: array
                        springcm:
                          description: |-
                            (Block List, Max: 1) SpringCM SSO configuration. (see below for nested schema)
                            SpringCM SSO configuration.
                          items:
                            properties:
                              acsUrl:
                                description: |-
                                  (String) SpringCM ACS URL, for example https://na11.springcm.com/atlas/sso/SSOEndpoint.ashx.
                                  SpringCM ACS URL, for example `https://na11.springcm.com/atlas/sso/SSOEndpoint.ashx`.
                                type: string
                            type: object
                          type: array
                        ssoIntegration:
                          description: |-
                            (Block List, Max: 1) Generic SSO configuration. (see below for nested schema)
                            Generic SSO configuration.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) Name of the client.
                                  SSO integration name.
                                type: string
                              version:
                                description: |-
                                  (String) SSO integration version installed.
                                  SSO integration version installed.
                                type: string
                            type: object
                          type: array
                        wams:
                          description: |-
                            (Block List, Max: 1) Windows Azure Mobile Services addon configuration. (see below for nested schema)
                            Windows Azure Mobile Services addon configuration.
                          items:
                            properties:
                              masterKeySecretRef:
                                description: |-
                                  (String, Sensitive) Your master key for Windows Azure Mobile Services.
                                  Your master key for Windows Azure Mobile Services.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          type: array
                        wsfed:
                          description: |-
                            Fed (WIF) addon indicator. Actual configuration is stored in callback and client_aliases properties on the client. (see below for nested schema)
                            WS-Fed (WIF) addon indicator. Actual configuration is stored in `callback` and `client_aliases` properties on the client.
                          items:
                            type: object
                          type: array
                        zendesk:
                          description: |-
                            (Block List, Max: 1) Zendesk SSO configuration. (see below for nested schema)
                            Zendesk SSO configuration.
                          items:
                            properties:
                              accountName:
                                description: |-
                                  org.blob.core.windows.net would be the account name acme-org.
                                  Zendesk account name. Usually the first segment in your Zendesk URL, for example `https://acme-org.zendesk.com` would be `acme-org`.
                                type: string
                            type: object
                          type: array
                        zoom:
                          description: |-
                            (Block List, Max: 1) Zoom SSO configuration. (see below for nested schema)
                            Zoom SSO configuration.
                          items:
                            properties:
                              account:
                                description: |-
                                  (String) Your New Relic Account ID found in your New Relic URL after the /accounts/ path, for example https://rpm.newrelic.com/accounts/123456/query would be 123456.
                                  Zoom account name. Usually the first segment of your Zoom URL, for example `https://acme-org.zoom.us` would be `acme-org`.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  allowedClients:
                    description: |-
                      (List of String) List of applications ID's that will be allowed to make delegation request. By default, all applications will be allowed.
                      List of applications ID's that will be allowed to make delegation request. By default, all applications will be allowed.
                    items:
                      type: string
                    type: array
                  allowedLogoutUrls:
                    description: |-
                      (List of String) URLs that Auth0 may redirect to after logout.
                      URLs that Auth0 may redirect to after logout.
                    items:
                      type: string
                    type: array
                  allowedOrigins:
                    description: |-
                      origin resource sharing. By default, all your callback URLs will be allowed.
                      URLs that represent valid origins for cross-origin resource sharing. By default, all your callback URLs will be allowed.
                    items:
                      type: string
                    type: array
                  appType:
                    description: |-
                      (String) Type of application the client represents. Possible values are: native, spa, regular_web, non_interactive, sso_integration. Specific SSO integrations types accepted as well are: rms, box, cloudbees, concur, dropbox, mscrm, echosign, egnyte, newrelic, office365, salesforce, sentry, sharepoint, slack, springcm, zendesk, zoom.
                      Type of application the client represents. Possible values are: `native`, `spa`, `regular_web`, `non_interactive`, `sso_integration`. Specific SSO integrations types accepted as well are: `rms`, `box`, `cloudbees`, `concur`, `dropbox`, `mscrm`, `echosign`, `egnyte`, `newrelic`, `office365`, `salesforce`, `sentry`, `sharepoint`, `slack`, `springcm`, `zendesk`, `zoom`.
                    type: string
                  callbacks:
                    description: |-
                      (List of String) URLs that Auth0 may call back to after a user authenticates for the client. Make sure to specify the protocol (https://) otherwise the callback may fail in some cases. With the exception of custom URI schemes for native clients, all callbacks should use protocol https://.
                      URLs that Auth0 may call back to after a user authenticates for the client. Make sure to specify the protocol (https://) otherwise the callback may fail in some cases. With the exception of custom URI schemes for native clients, all callbacks should use protocol https://.
                    items:
                      type: string
                    type: array
                  clientAliases:
                    description: |-
                      (List of String) List of audiences/realms for SAML protocol. Used by the wsfed addon.
                      List of audiences/realms for SAML protocol. Used by the wsfed addon.
                    items:
                      type: string
                    type: array
                  clientMetadata:
                    additionalProperties:
                      type: string
                    description: 'Metadata associated with the client, in the form
                      of an object with string values (max 255 chars). Maximum of
                      10 metadata properties allowed. Field names (max 255 chars)
                      are alphanumeric and may only include the following special
                      characters: `:,-+=_*?"/\()<>@ [Tab] [Space]`.'
                    type: object
                    x-kubernetes-map-type: granular
                  complianceLevel:
                    description: |-
                      (String) Defines the compliance level for this client, which may restrict it's capabilities. Can be one of none, fapi1_adv_pkj_par, fapi1_adv_mtls_par.
                      Defines the compliance level for this client, which may restrict it's capabilities. Can be one of `none`, `fapi1_adv_pkj_par`, `fapi1_adv_mtls_par`.
                    type: string
                  crossOriginAuth:
                    description: |-
                      origin authentication requests (true) or it is not allowed to make such requests (false).
                      Whether this client can be used to make cross-origin authentication requests (`true`) or it is not allowed to make such requests (`false`).
                    type: boolean
                  crossOriginLoc:
                    description: |-
                      origin verification takes place for the cross-origin auth flow when performing authentication in your own domain instead of Auth0 Universal Login page.
                      URL of the location in your site where the cross-origin verification takes place for the cross-origin auth flow when performing authentication in your own domain instead of Auth0 Universal Login page.
                    type: string
                  customLoginPage:
                    description: |-
                      (String) The content (HTML, CSS, JS) of the custom login page.
                      The content (HTML, CSS, JS) of the custom login page.
                    type: string
                  customLoginPageOn:
                    description: |-
                      (Boolean) Indicates whether a custom login page is to be used.
                      Indicates whether a custom login page is to be used.
                    type: boolean
                  defaultOrganization:
                    description: |-
                      (Block List, Max: 1) Configure and associate an organization with the Client (see below for nested schema)
                      Configure and associate an organization with the Client
                    items:
                      properties:
                        disable:
                          description: |-
                            (Boolean) If set, the default_organization will be removed.
                            If set, the `default_organization` will be removed.
                          type: boolean
                        flows:
                          description: |-
                            (List of String) Definition of the flow that needs to be configured. Eg. client_credentials
                            Definition of the flow that needs to be configured. Eg. client_credentials
                          items:
                            type: string
                          type: array
                        organizationId:
                          description: |-
                            (String) The unique identifier of the organization
                            The unique identifier of the organization
                          type: string
                      type: object
                    type: array
                  description:
                    description: |-
                      (String) Description of the purpose of the client.
                      Description of the purpose of the client.
                    type: string
                  encryptionKey:
                    additionalProperties:
                      type: string
                    description: |-
                      Fed responses with this client.
                      Encryption used for WS-Fed responses with this client.
                    type: object
                    x-kubernetes-map-type: granular
                  formTemplate:
                    description: |-
                      Federation.
                      HTML form template to be used for WS-Federation.
                    type: string
                  grantTypes:
                    description: |-
                      (List of String) Types of grants that this client is authorized to use.
                      Types of grants that this client is authorized to use.
                    items:
                      type: string
                    type: array
                  initiateLoginUri:
                    description: |-
                      (String) Initiate login URI. Must be HTTPS or an empty string.
                      Initiate login URI. Must be HTTPS or an empty string.
                    type: string
                  isFirstParty:
                    description: |-
                      party client.Defaults to true from the API
                      Indicates whether this client is a first-party client.Defaults to true from the API
                    type: boolean
                  isTokenEndpointIpHeaderTrusted:
                    description: |-
                      (Boolean) Indicates whether the token endpoint IP header is trusted. Requires the authentication method to be set to client_secret_post or client_secret_basic. Setting this property when creating the resource, will default the authentication method to client_secret_post. To change the authentication method to client_secret_basic use the auth0_client_credentials resource.
                      Indicates whether the token endpoint IP header is trusted. Requires the authentication method to be set to `client_secret_post` or `client_secret_basic`. Setting this property when creating the resource, will default the authentication method to `client_secret_post`. To change the authentication method to `client_secret_basic` use the `auth0_client_credentials` resource.
                    type: boolean
                  jwtConfiguration:
                    description: |-
                      (Block List, Max: 1) Configuration settings for the JWTs issued for this client. (see below for nested schema)
                      Configuration settings for the JWTs issued for this client.
                    items:
                      properties:
                        alg:
                          description: |-
                            (String) Algorithm used to sign JWTs. Can be one of HS256, RS256, PS256.
                            Algorithm used to sign JWTs. Can be one of `HS256`, `RS256`, `PS256`.
                          type: string
                        lifetimeInSeconds:
                          description: |-
                            (Number) AWS token lifetime in seconds.
                            Number of seconds during which the JWT will be valid.
                          type: number
                        scopes:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Permissions (scopes) included in JWTs.
                            Permissions (scopes) included in JWTs.
                          type: object
                          x-kubernetes-map-type: granular
                        secretEncoded:
                          description: |-
                            encoded.
                            Indicates whether the client secret is Base64-encoded.
                          type: boolean
                      type: object
                    type: array
                  logoUri:
                    description: |-
                      (String) URL of the logo for the client. Recommended size is 150px x 150px. If none is set, the default badge for the application type will be shown.
                      URL of the logo for the client. Recommended size is 150px x 150px. If none is set, the default badge for the application type will be shown.
                    type: string
                  mobile:
                    description: |-
                      (Block List, Max: 1) Additional configuration for native mobile apps. (see below for nested schema)
                      Additional configuration for native mobile apps.
                    items:
                      properties:
                        android:
                          description: |-
                            (Block List, Max: 1) Configuration settings for Android native apps. (see below for nested schema)
                            Configuration settings for Android native apps.
                          items:
                            properties:
                              appPackageName:
                                description: (String)
                                type: string
                              sha256CertFingerprints:
                                description: (List of String)
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        ios:
                          description: |-
                            (Block List, Max: 1) Configuration settings for i0S native apps. (see below for nested schema)
                            Configuration settings for i0S native apps.
                          items:
                            properties:
                              appBundleIdentifier:
                                description: (String)
                                type: string
                              teamId:
                                description: (String)
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  nativeSocialLogin:
                    description: |-
                      (Block List, Max: 1) Configuration settings to toggle native social login for mobile native applications. Once this is set it must stay set, with both resources set to false in order to change the app_type. (see below for nested schema)
                      Configuration settings to toggle native social login for mobile native applications. Once this is set it must stay set, with both resources set to `false` in order to change the `app_type`.
                    items:
                      properties:
                        apple:
                          description: '(Block List, Max: 1) (see below for nested
                            schema)'
                          items:
                            properties:
                              enabled:
                                description: (Boolean)
                                type: boolean
                            type: object
                          type: array
                        facebook:
                          description: '(Block List, Max: 1) (see below for nested
                            schema)'
                          items:
                            properties:
                              enabled:
                                description: (Boolean)
                                type: boolean
                            type: object
                          type: array
                        google:
                          description: '(Block List, Max: 1) (see below for nested
                            schema)'
                          items:
                            properties:
                              enabled:
                                description: (Boolean)
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  oidcBackchannelLogoutUrls:
                    description: |-
                      (Set of String, Deprecated) Set of URLs that are valid to call back from Auth0 for OIDC backchannel logout. Currently only one URL is allowed.
                      Set of URLs that are valid to call back from Auth0 for OIDC backchannel logout. Currently only one URL is allowed.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  oidcConformant:
                    description: |-
                      (Boolean) Indicates whether this client will conform to strict OIDC specifications.
                      Indicates whether this client will conform to strict OIDC specifications.
                    type: boolean
                  oidcLogout:
                    description: |-
                      (Block List, Max: 1) Configure OIDC logout for the Client (see below for nested schema)
                      Configure OIDC logout for the Client
                    items:
                      properties:
                        backchannelLogoutInitiators:
                          description: |-
                            (Block List, Max: 1) Configure OIDC logout initiators for the Client (see below for nested schema)
                            Configure OIDC logout initiators for the Client
                          items:
                            properties:
                              mode:
                                description: |-
                                  (String) Determines the configuration method for enabling initiators. custom enables only the initiators listed in the backchannel_logout_selected_initiators set, all enables all current and future initiators.
                                  Determines the configuration method for enabling initiators. `custom` enables only the initiators listed in the backchannel_logout_selected_initiators set, `all` enables all current and future initiators.
                                type: string
                              selectedInitiators:
                                description: |-
                                  (Set of String) Contains the list of initiators to be enabled for the given client.
                                  Contains the list of initiators to be enabled for the given client.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        backchannelLogoutUrls:
                          description: |-
                            (Set of String) Set of URLs that are valid to call back from Auth0 for OIDC backchannel logout. Currently only one URL is allowed.
                            Set of URLs that are valid to call back from Auth0 for OIDC backchannel logout. Currently only one URL is allowed.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  organizationRequireBehavior:
                    description: |-
                      (String) Defines how to proceed during an authentication transaction when organization_usage = "require". Can be no_prompt (default), pre_login_prompt or  post_login_prompt.
                      Defines how to proceed during an authentication transaction when `organization_usage = "require"`. Can be `no_prompt` (default), `pre_login_prompt` or  `post_login_prompt`.
                    type: string
                  organizationUsage:
                    description: |-
                      (String) Defines how to proceed during an authentication transaction with regards to an organization. Can be deny (default), allow or require.
                      Defines how to proceed during an authentication transaction with regards to an organization. Can be `deny` (default), `allow` or `require`.
                    type: string
                  refreshToken:
                    description: |-
                      (Block List, Max: 1) Configuration settings for the refresh tokens issued for this client. (see below for nested schema)
                      Configuration settings for the refresh tokens issued for this client.
                    items:
                      properties:
                        expirationType:
                          description: |-
                            expiring. Whether a refresh token will expire based on an absolute lifetime, after which the token can no longer be used. If rotation is rotating, this must be set to expiring.
                            Options include `expiring`, `non-expiring`. Whether a refresh token will expire based on an absolute lifetime, after which the token can no longer be used. If rotation is `rotating`, this must be set to `expiring`.
                          type: string
                        idleTokenLifetime:
                          description: |-
                            (Number) The time in seconds after which inactive refresh tokens will expire.
                            The time in seconds after which inactive refresh tokens will expire.
                          type: number
                        infiniteIdleTokenLifetime:
                          description: |-
                            (Boolean) Whether inactive refresh tokens should remain valid indefinitely.
                            Whether inactive refresh tokens should remain valid indefinitely.
                          type: boolean
                        infiniteTokenLifetime:
                          description: |-
                            (Boolean) Whether refresh tokens should remain valid indefinitely. If false, token_lifetime should also be set.
                            Whether refresh tokens should remain valid indefinitely. If false, `token_lifetime` should also be set.
                          type: boolean
                        leeway:
                          description: |-
                            (Number) The amount of time in seconds in which a refresh token may be reused without triggering reuse detection.
                            The amount of time in seconds in which a refresh token may be reused without triggering reuse detection.
                          type: number
                        policies:
                          description: |-
                            resource refresh token exchange (MRRT), defining how refresh tokens can be used across different resource servers (see below for nested schema)
                            A collection of policies governing multi-resource refresh token exchange (MRRT), defining how refresh tokens can be used across different resource servers
                          items:
                            properties:
                              audience:
                                description: |-
                                  (String) Audience of the SAML Assertion. Default will be the Issuer on SAMLRequest.
                                  The identifier of the resource server to which the Multi Resource Refresh Token Policy applies
                                type: string
                              scope:
                                description: |-
                                  (String) Requested scope for SAP APIs.
                                  The resource server permissions granted under the Multi Resource Refresh Token Policy, defining the context in which an access token can be used
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        rotationType:
                          description: |-
                            rotating. When rotating, exchanging a refresh token will cause a new refresh token to be issued and the existing token will be invalidated. This allows for automatic detection of token reuse if the token is leaked.
                            Options include `rotating`, `non-rotating`. When `rotating`, exchanging a refresh token will cause a new refresh token to be issued and the existing token will be invalidated. This allows for automatic detection of token reuse if the token is leaked.
                          type: string
                        tokenLifetime:
                          description: |-
                            (Number) The absolute lifetime of a refresh token in seconds.
                            The absolute lifetime of a refresh token in seconds.
                          type: number
                      type: object
                    type: array
                  requireProofOfPossession:
                    description: |-
                      of-Possession mandatory for this client.
                      Makes the use of Proof-of-Possession mandatory for this client.
                    type: boolean
                  requirePushedAuthorizationRequests:
                    description: |-
                      (Boolean) Makes the use of Pushed Authorization Requests mandatory for this client. This feature currently needs to be enabled on the tenant in order to make use of it.
                      Makes the use of Pushed Authorization Requests mandatory for this client. This feature currently needs to be enabled on the tenant in order to make use of it.
                    type: boolean
                  sessionTransfer:
                    description: '(Block List, Max: 1) (see below for nested schema)'
                    items:
                      properties:
                        allowRefreshToken:
                          description: |-
                            (Boolean) Indicates whether the application is allowed to use a refresh token when using a session_transfer_token session.
                            Indicates whether the application is allowed to use a refresh token when using a session_transfer_token session.
                          type: boolean
                        allowedAuthenticationMethods:
                          description: (Set of String)
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        canCreateSessionTransferToken:
                          description: |-
                            (Boolean) Indicates whether the application(Native app) can use the Token Exchange endpoint to create a session_transfer_token
                            Indicates whether the application(Native app) can use the Token Exchange endpoint to create a session_transfer_token
                          type: boolean
                        enforceDeviceBinding:
                          description: |-
                            (String) Configures the level of device binding enforced when a session_transfer_token is consumed. Can be one of ip, asn or none.
                            Configures the level of device binding enforced when a session_transfer_token is consumed. Can be one of `ip`, `asn` or `none`.
                          type: string
                      type: object
                    type: array
                  sso:
                    description: |-
                      On (true) or whether the identity provider will (false).
                      Applies only to SSO clients and determines whether Auth0 will handle Single Sign-On (true) or whether the identity provider will (false).
                    type: boolean
                  ssoDisabled:
                    description: |-
                      (Boolean) Indicates whether or not SSO is disabled.
                      Indicates whether or not SSO is disabled.
                    type: boolean
                  tokenExchange:
                    description: |-
                      (Block List, Max: 1) Allows configuration for token exchange (see below for nested schema)
                      Allows configuration for token exchange
                    items:
                      properties:
                        allowAnyProfileOfType:
                          description: |-
                            (List of String) List of allowed profile types for token exchange
                            List of allowed profile types for token exchange
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  tokenQuota:
                    description: |-
                      (Block List, Max: 1) The token quota configuration. (see below for nested schema)
                      The token quota configuration.
                    items:
                      properties:
                        clientCredentials:
                          description: |-
                            (Block List, Min: 1, Max: 1) The token quota configuration for client credentials. (see below for nested schema)
                            The token quota configuration for client credentials.
                          items:
                            properties:
                              enforce:
                                description: |-
                                  (Boolean) If enabled, the quota will be enforced and requests in excess of the quota will fail. If disabled, the quota will not be enforced, but notifications for requests exceeding the quota will be available in logs.
                                  If enabled, the quota will be enforced and requests in excess of the quota will fail. If disabled, the quota will not be enforced, but notifications for requests exceeding the quota will be available in logs.
                                type: boolean
                              perDay:
                                description: |-
                                  (Number) Maximum number of issued tokens per day
                                  Maximum number of issued tokens per day
                                type: number
                              perHour:
                                description: |-
                                  (Number) Maximum number of issued tokens per hour
                                  Maximum number of issued tokens per hour
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  webOrigins:
                    description: |-
                      (List of String) URLs that represent valid web origins for use with web message response mode.
                      URLs that represent valid web origins for use with web message response mode.
                    items:
                      type: string
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ClientStatus defines the observed state of Client.
            properties:
              atProvider:
                properties:
                  addons:
                    description: |-
                      (Block List, Max: 1) Addons enabled for this client and their associated configurations. (see below for nested schema)
                      Addons enabled for this client and their associated configurations.
                    items:
                      properties:
                        aws:
                          description: |-
                            (Block List, Max: 1) AWS Addon configuration. (see below for nested schema)
                            AWS Addon configuration.
                          items:
                            properties:
                              lifetimeInSeconds:
                                description: |-
                                  (Number) AWS token lifetime in seconds.
                                  AWS token lifetime in seconds.
                                type: number
                              principal:
                                description: |-
                                  provider/idpname.
                                  AWS principal ARN, for example `arn:aws:iam::010616021751:saml-provider/idpname`.
                                type: string
                              role:
                                description: |-
                                  (String) AWS role ARN, for example arn:aws:iam::010616021751:role/foo.
                                  AWS role ARN, for example `arn:aws:iam::010616021751:role/foo`.
                                type: string
                            type: object
                          type: array
                        azureBlob:
                          description: |-
                            (Block List, Max: 1) Azure Blob Storage Addon configuration. (see below for nested schema)
                            Azure Blob Storage Addon configuration.
                          items:
                            properties:
                              accountName:
                                description: |-
                                  org.blob.core.windows.net would be the account name acme-org.
                                  Your Azure storage account name. Usually first segment in your Azure storage URL, for example `https://acme-org.blob.core.windows.net` would be the account name `acme-org`.
                                type: string
                              blobDelete:
                                description: |-
                                  (Boolean) Indicates if the issued token has permission to delete the blob.
                                  Indicates if the issued token has permission to delete the blob.
                                type: boolean
                              blobName:
                                description: |-
                                  blob. If blank the computed SAS will apply to the entire storage container.
                                  Entity to request a token for, such as `my-blob`. If blank the computed SAS will apply to the entire storage container.
                                type: string
                              blobRead:
                                description: |-
                                  (Boolean) Indicates if the issued token has permission to read the content, properties, metadata and block list. Use the blob as the source of a copy operation.
                                  Indicates if the issued token has permission to read the content, properties, metadata and block list. Use the blob as the source of a copy operation.
                                type: boolean
                              blobWrite:
                                description: |-
                                  (Boolean) Indicates if the issued token has permission to create or write content, properties, metadata, or block list. Snapshot or lease the blob. Resize the blob (page blob only). Use the blob as the destination of a copy operation within the same account.
                                  Indicates if the issued token has permission to create or write content, properties, metadata, or block list. Snapshot or lease the blob. Resize the blob (page blob only). Use the blob as the destination of a copy operation within the same account.
                                type: boolean
                              containerDelete:
                                description: |-
                                  (Boolean) Indicates if issued token has permission to delete any blob in the container.
                                  Indicates if issued token has permission to delete any blob in the container.
                                type: boolean
                              containerList:
                                description: |-
                                  (Boolean) Indicates if the issued token has permission to list blobs in the container.
                                  Indicates if the issued token has permission to list blobs in the container.
                                type: boolean
                              containerName:
                                description: |-
                                  container.
                                  Container to request a token for, such as `my-container`.
                                type: string
                              containerRead:
                                description: |-
                                  (Boolean) Indicates if the issued token has permission to read the content, properties, metadata or block list of any blob in the container. Use any blob in the container as the source of a copy operation.
                                  Indicates if the issued token has permission to read the content, properties, metadata or block list of any blob in the container. Use any blob in the container as the source of a copy operation.
                                type: boolean
                              containerWrite:
                                description: |-
                                  (Boolean) Indicates that for any blob in the container if the issued token has permission to create or write content, properties, metadata, or block list. Snapshot or lease the blob. Resize the blob (page blob only). Use the blob as the destination of a copy operation within the same account.
                                  Indicates that for any blob in the container if the issued token has permission to create or write content, properties, metadata, or block list. Snapshot or lease the blob. Resize the blob (page blob only). Use the blob as the destination of a copy operation within the same account.
                                type: boolean
                              expiration:
                                description: |-
                                  (Number) Expiration in minutes for the generated token (default of 5 minutes).
                                  Expiration in minutes for the generated token (default of 5 minutes).
                                type: number
                              signedIdentifier:
                                description: |-
                                  (String) Shared access policy identifier defined in your storage account resource.
                                  Shared access policy identifier defined in your storage account resource.
                                type: string
                            type: object
                          type: array
                        azureSb:
                          description: |-
                            (Block List, Max: 1) Azure Storage Bus Addon configuration. (see below for nested schema)
                            Azure Storage Bus Addon configuration.
                          items:
                            properties:
                              entityPath:
                                description: |-
                                  queue.
                                  Entity you want to request a token for, such as `my-queue`.
                                type: string
                              expiration:
                                description: |-
                                  (Number) Expiration in minutes for the generated token (default of 5 minutes).
                                  Optional expiration in minutes for the generated token. Defaults to 5 minutes.
                                type: number
                              namespace:
                                description: |-
                                  org.servicebus.windows.net would be acme-org).
                                  Your Azure Service Bus namespace. Usually the first segment of your Service Bus URL (for example `https://acme-org.servicebus.windows.net` would be `acme-org`).
                                type: string
                              sasKeyName:
                                description: |-
                                  (String) Your shared access policy name defined in your Service Bus entity.
                                  Your shared access policy name defined in your Service Bus entity.
                                type: string
                            type: object
                          type: array
                        box:
                          description: |-
                            (Block List, Max: 1) Box SSO indicator (no configuration settings needed for Box SSO). (see below for nested schema)
                            Box SSO indicator (no configuration settings needed for Box SSO).
                          items:
                            type: object
                          type: array
                        cloudbees:
                          description: |-
                            (Block List, Max: 1) CloudBees SSO indicator (no configuration settings needed for CloudBees SSO). (see below for nested schema)
                            CloudBees SSO indicator (no configuration settings needed for CloudBees SSO).
                          items:
                            type: object
                          type: array
                        concur:
                          description: |-
                            (Block List, Max: 1) Concur SSO indicator (no configuration settings needed for Concur SSO). (see below for nested schema)
                            Concur SSO indicator (no configuration settings needed for Concur SSO).
                          items:
                            type: object
                          type: array
                        dropbox:
                          description: |-
                            (Block List, Max: 1) Dropbox SSO indicator (no configuration settings needed for Dropbox SSO). (see below for nested schema)
                            Dropbox SSO indicator (no configuration settings needed for Dropbox SSO).
                          items:
                            type: object
                          type: array
                        echosign:
                          description: |-
                            (Block List, Max: 1) Adobe EchoSign SSO configuration. (see below for nested schema)
                            Adobe EchoSign SSO configuration.
                          items:
                            properties:
                              domain:
                                description: |-
                                  org.echosign.com would be acme-org.
                                  Your custom domain found in your EchoSign URL, for example `https://acme-org.echosign.com` would be `acme-org`.
                                type: string
                            type: object
                          type: array
                        egnyte:
                          description: |-
                            (Block List, Max: 1) Egnyte SSO configuration. (see below for nested schema)
                            Egnyte SSO configuration.
                          items:
                            properties:
                              domain:
                                description: |-
                                  org.echosign.com would be acme-org.
                                  Your custom domain found in your Egnyte URL, for example `https://acme-org.echosign.com` would be `acme-org`.
                                type: string
                            type: object
                          type: array
                        firebase:
                          description: |-
                            (Block List, Max: 1) Google Firebase addon configuration. (see below for nested schema)
                            Google Firebase addon configuration.
                          items:
                            properties:
                              clientEmail:
                                description: |-
                                  (String) ID of the Service Account you have created (shown as client_email in the generated JSON file, SDK v3+ tokens only).
                                  ID of the Service Account you have created (shown as `client_email` in the generated JSON file, SDK v3+ tokens only).
                                type: string
                              lifetimeInSeconds:
                                description: |-
                                  (Number) AWS token lifetime in seconds.
                                  Optional expiration in seconds for the generated token. Defaults to 3600 seconds (SDK v3+ tokens only).
                                type: number
                            type: object
                          type: array
                        layer:
                          description: |-
                            (Block List, Max: 1) Layer addon configuration. (see below for nested schema)
                            Layer addon configuration.
                          items:
                            properties:
                              expiration:
                                description: |-
                                  (Number) Expiration in minutes for the generated token (default of 5 minutes).
                                  Optional expiration in minutes for the generated token. Defaults to 5 minutes.
                                type: number
                              principal:
                                description: |-
                                  provider/idpname.
                                  Name of the property used as the unique user ID in Layer. If not specified `user_id` is used.
                                type: string
                              providerId:
                                description: |-
                                  (String) Provider ID of your Layer account.
                                  Provider ID of your Layer account.
                                type: string
                            type: object
                          type: array
                        mscrm:
                          description: |-
                            (Block List, Max: 1) Microsoft Dynamics CRM SSO configuration. (see below for nested schema)
                            Microsoft Dynamics CRM SSO configuration.
                          items:
                            properties:
                              url:
                                description: |-
                                  (String) Microsoft Dynamics CRM application URL.
                                  Microsoft Dynamics CRM application URL.
                                type: string
                            type: object
                          type: array
                        newrelic:
                          description: |-
                            (Block List, Max: 1) New Relic SSO configuration. (see below for nested schema)
                            New Relic SSO configuration.
                          items:
                            properties:
                              account:
                                description: |-
                                  (String) Your New Relic Account ID found in your New Relic URL after the /accounts/ path, for example https://rpm.newrelic.com/accounts/123456/query would be 123456.
                                  Your New Relic Account ID found in your New Relic URL after the `/accounts/` path, for example `https://rpm.newrelic.com/accounts/123456/query` would be `123456`.
                                type: string
                            type: object
                          type: array
                        office365:
                          description: |-
                            (Block List, Max: 1) Microsoft Office 365 SSO configuration. (see below for nested schema)
                            Microsoft Office 365 SSO configuration.
                          items:
                            properties:
                              connection:
                                description: |-
                                  configured Office 365 tenant.
                                  Optional Auth0 database connection for testing an already-configured Office 365 tenant.
                                type: string
                              domain:
                                description: |-
                                  org.echosign.com would be acme-org.
                                  Your Office 365 domain name, for example `acme-org.com`.
                                type: string
                            type: object
                          type: array
                        rms:
                          description: |-
                            (Block List, Max: 1) Active Directory Rights Management Service SSO configuration. (see below for nested schema)
                            Active Directory Rights Management Service SSO configuration.
                          items:
                            properties:
                              url:
                                description: |-
                                  (String) Microsoft Dynamics CRM application URL.
                                  URL of your Rights Management Server. It can be internal or external, but users will have to be able to reach it.
                                type: string
                            type: object
                          type: array
                        salesforce:
                          description: |-
                            (Block List, Max: 1) Salesforce SSO configuration. (see below for nested schema)
                            Salesforce SSO configuration.
                          items:
                            properties:
                              entityId:
                                description: |-
                                  org.com.
                                  Arbitrary logical URL that identifies the Saleforce resource, for example `https://acme-org.com`.
                                type: string
                            type: object
                          type: array
                        salesforceApi:
                          description: |-
                            (Block List, Max: 1) Salesforce API addon configuration. (see below for nested schema)
                            Salesforce API addon configuration.
                          items:
                            properties:
                              communityName:
                                description: |-
                                  (String) Community name.
                                  Community name.
                                type: string
                              communityUrlSection:
                                description: |-
                                  (String) Community URL section.
                                  Community URL section.
                                type: string
                            type: object
                          type: array
                        salesforceSandboxApi:
                          description: |-
                            (Block List, Max: 1) Salesforce Sandbox addon configuration. (see below for nested schema)
                            Salesforce Sandbox addon configuration.
                          items:
                            properties:
                              communityName:
                                description: |-
                                  (String) Community name.
                                  Community name.
                                type: string
                              communityUrlSection:
                                description: |-
                                  (String) Community URL section.
                                  Community URL section.
                                type: string
                            type: object
                          type: array
                        samlp:
                          description: |-
                            on. (see below for nested schema)
                            Configuration settings for a SAML add-on.
                          items:
                            properties:
                              audience:
                                description: |-
                                  (String) Audience of the SAML Assertion. Default will be the Issuer on SAMLRequest.
                                  Audience of the SAML Assertion. Default will be the Issuer on SAMLRequest.
                                type: string
                              authnContextClassRef:
                                description: |-
                                  (String) Class reference of the authentication context.
                                  Class reference of the authentication context.
                                type: string
                              binding:
                                description: |-
                                  (String) Protocol binding used for SAML logout responses.
                                  Protocol binding used for SAML logout responses.
                                type: string
                              createUpnClaim:
                                description: |-
                                  (Boolean) Indicates whether a UPN claim should be created. Defaults to true.
                                  Indicates whether a UPN claim should be created. Defaults to `true`.
                                type: boolean
                              destination:
                                description: |-
                                  (String) Destination of the SAML Response. If not specified, it will be AssertionConsumerUrl of SAMLRequest or callback URL if there was no SAMLRequest.
                                  Destination of the SAML Response. If not specified, it will be `AssertionConsumerUrl` of SAMLRequest or callback URL if there was no SAMLRequest.
                                type: string
                              digestAlgorithm:
                                description: |-
                                  (String) Algorithm used to calculate the digest of the SAML Assertion or response. Options include sha1 and sha256. Defaults to sha1.
                                  Algorithm used to calculate the digest of the SAML Assertion or response. Options include `sha1` and `sha256`. Defaults to `sha1`.
                                type: string
                              flexibleMappings:
                                description: |-
                                  (String) This is a supporting attribute to mappings field.Please note this is an experimental field. It should only be used when needed to send a map with keys as slices.
                                  This is a supporting attribute to `mappings` field.Please note this is an experimental field. It should only be used when needed to send a map with keys as slices.
                                type: string
                              includeAttributeNameFormat:
                                description: |-
                                  (Boolean) Indicates whether or not we should infer the NameFormat based on the attribute name. If set to false, the attribute NameFormat is not set in the assertion. Defaults to true.
                                  Indicates whether or not we should infer the NameFormat based on the attribute name. If set to `false`, the attribute NameFormat is not set in the assertion. Defaults to `true`.
                                type: boolean
                              issuer:
                                description: |-
                                  (String) Issuer of the SAML Assertion.
                                  Issuer of the SAML Assertion.
                                type: string
                              lifetimeInSeconds:
                                description: |-
                                  (Number) AWS token lifetime in seconds.
                                  Number of seconds during which the token is valid. Defaults to `3600` seconds.
                                type: number
                              logout:
                                description: |-
                                  (Block List, Max: 1) Configuration settings for logout. (see below for nested schema)
                                  Configuration settings for logout.
                                items:
                                  properties:
                                    callback:
                                      description: |-
                                        (String) The service provider (client application)'s Single Logout Service URL, where Auth0 will send logout requests and responses.
                                        The service provider (client application)'s Single Logout Service URL, where Auth0 will send logout requests and responses.
                                      type: string
                                    sloEnabled:
                                      description: |-
                                        (Boolean) Controls whether Auth0 should notify service providers of session termination.
                                        Controls whether Auth0 should notify service providers of session termination.
                                      type: boolean
                                  type: object
                                type: array
                              mapIdentities:
                                description: |-
                                  (Boolean) Indicates whether or not to add additional identity information in the token, such as the provider used and the access_token, if available. Defaults to true.
                                  Indicates whether or not to add additional identity information in the token, such as the provider used and the `access_token`, if available. Defaults to `true`.
                                type: boolean
                              mapUnknownClaimsAsIs:
                                description: |-
                                  (Boolean) Indicates whether to add a prefix of http://schema.auth0.com to any claims that are not mapped to the common profile when passed through in the output assertion. Defaults to false.
                                  Indicates whether to add a prefix of `http://schema.auth0.com` to any claims that are not mapped to the common profile when passed through in the output assertion. Defaults to `false`.
                                type: boolean
                              mappings:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Mappings between the Auth0 user profile property name (name) and the output attributes on the SAML attribute in the assertion (value).
                                  Mappings between the Auth0 user profile property name (`name`) and the output attributes on the SAML attribute in the assertion (`value`).
                                type: object
                                x-kubernetes-map-type: granular
                              nameIdentifierFormat:
                                description: |-
                                  format:unspecified.
                                  Format of the name identifier. Defaults to `urn:oasis:names:tc:SAML:1.1:nameid-format:unspecified`.
                                type: string
                              nameIdentifierProbes:
                                description: |-
                                  (List of String) Attributes that can be used for Subject/NameID. Auth0 will try each of the attributes of this array in order and use the first value it finds.
                                  Attributes that can be used for Subject/NameID. Auth0 will try each of the attributes of this array in order and use the first value it finds.
                                items:
                                  type: string
                                type: array
                              passthroughClaimsWithNoMapping:
                                description: |-
                                  (Boolean) Indicates whether or not to passthrough claims that are not mapped to the common profile in the output assertion. Defaults to true.
                                  Indicates whether or not to passthrough claims that are not mapped to the common profile in the output assertion. Defaults to `true`.
                                type: boolean
                              recipient:
                                description: |-
                                  (String) Recipient of the SAML Assertion (SubjectConfirmationData). Default is AssertionConsumerUrl on SAMLRequest or callback URL if no SAMLRequest was sent.
                                  Recipient of the SAML Assertion (SubjectConfirmationData). Default is `AssertionConsumerUrl` on SAMLRequest or callback URL if no SAMLRequest was sent.
                                type: string
                              signResponse:
                                description: |-
                                  (Boolean) Indicates whether or not the SAML Response should be signed instead of the SAML Assertion.
                                  Indicates whether or not the SAML Response should be signed instead of the SAML Assertion.
                                type: boolean
                              signatureAlgorithm:
                                description: |-
                                  sha1 and rsa-sha256. Defaults to rsa-sha1.
                                  Algorithm used to sign the SAML Assertion or response. Options include `rsa-sha1` and `rsa-sha256`. Defaults to `rsa-sha1`.
                                type: string
                              signingCert:
                                type: string
                              typedAttributes:
                                description: |-
                                  (Boolean) Indicates whether or not we should infer the xs:type of the element. Types include xs:string, xs:boolean, xs:double, and xs:anyType. When set to false, all xs:type are xs:anyType. Defaults to true.
                                  Indicates whether or not we should infer the `xs:type` of the element. Types include `xs:string`, `xs:boolean`, `xs:double`, and `xs:anyType`. When set to `false`, all `xs:type` are `xs:anyType`. Defaults to `true`.
                                type: boolean
                            type: object
                          type: array
                        sapApi:
                          description: |-
                            (Block List, Max: 1) SAP API addon configuration. (see below for nested schema)
                            SAP API addon configuration.
                          items:
                            properties:
                              clientId:
                                description: |-
                                  (String) The ID of the client.
                                  If activated in the OAuth 2.0 client configuration (transaction `SOAUTH2) the SAML attribute `client_id` must be set and equal the `client_id` form parameter of the access token request.
                                type: string
                              nameIdentifierFormat:
                                description: |-
                                  format:unspecified.
                                  NameID element of the Subject which can be used to express the user's identity. Defaults to `urn:oasis:names:tc:SAML:1.1:nameid-format:unspecified`.
                                type: string
                              scope:
                                description: |-
                                  (String) Requested scope for SAP APIs.
                                  Requested scope for SAP APIs.
                                type: string
                              tokenEndpointUrl:
                                description: |-
                                  (String) The OAuth2 token endpoint URL of your SAP OData server.
                                  The OAuth2 token endpoint URL of your SAP OData server.
                                type: string
                              usernameAttribute:
                                description: |-
                                  (String) Name of the property in the user object that maps to a SAP username, for example email.
                                  Name of the property in the user object that maps to a SAP username, for example `email`.
                                type: string
                            type: object
                          type: array
                        sentry:
                          description: |-
                            (Block List, Max: 1) Sentry SSO configuration. (see below for nested schema)
                            Sentry SSO configuration.
                          items:
                            properties:
                              baseUrl:
                                description: |-
                                  (String) URL prefix only if running Sentry Community Edition, otherwise leave empty.
                                  URL prefix only if running Sentry Community Edition, otherwise leave empty.
                                type: string
                              orgSlug:
                                description: |-
                                  org/ would be acme-org.
                                  Generated slug for your Sentry organization. Found in your Sentry URL, for example `https://sentry.acme.com/acme-org/` would be `acme-org`.
                                type: string
                            type: object
                          type: array
                        sharepoint:
                          description: |-
                            (Block List, Max: 1) SharePoint SSO configuration. (see below for nested schema)
                            SharePoint SSO configuration.
                          items:
                            properties:
                              externalUrl:
                                description: |-
                                  (List of String) External SharePoint application URLs if exposed to the Internet.
                                  External SharePoint application URLs if exposed to the Internet.
                                items:
                                  type: string
                                type: array
                              url:
                                description: |-
                                  (String) Microsoft Dynamics CRM application URL.
                                  Internal SharePoint application URL.
                                type: string
                            type: object
                          type: array
                        slack:
                          description: |-
                            org.slack.com would be acme-org. (see below for nested schema)
                            Slack team or workspace name usually first segment in your Slack URL, for example `https://acme-org.slack.com` would be `acme-org`.
                          items:
                            properties:
                              team:
                                description: |-
                                  (String) Slack team name.
                                  Slack team name.
                                type: string
                            type: object
                          type: array
                        springcm:
                          description: |-
                            (Block List, Max: 1) SpringCM SSO configuration. (see below for nested schema)
                            SpringCM SSO configuration.
                          items:
                            properties:
                              acsUrl:
                                description: |-
                                  (String) SpringCM ACS URL, for example https://na11.springcm.com/atlas/sso/SSOEndpoint.ashx.
                                  SpringCM ACS URL, for example `https://na11.springcm.com/atlas/sso/SSOEndpoint.ashx`.
                                type: string
                            type: object
                          type: array
                        ssoIntegration:
                          description: |-
                            (Block List, Max: 1) Generic SSO configuration. (see below for nested schema)
                            Generic SSO configuration.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) Name of the client.
                                  SSO integration name.
                                type: string
                              version:
                                description: |-
                                  (String) SSO integration version installed.
                                  SSO integration version installed.
                                type: string
                            type: object
                          type: array
                        wams:
                          description: |-
                            (Block List, Max: 1) Windows Azure Mobile Services addon configuration. (see below for nested schema)
                            Windows Azure Mobile Services addon configuration.
                          items:
                            properties:
                              masterKeySecretRef:
                                description: |-
                                  (String, Sensitive) Your master key for Windows Azure Mobile Services.
                                  Your master key for Windows Azure Mobile Services.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          type: array
                        wsfed:
                          description: |-
                            Fed (WIF) addon indicator. Actual configuration is stored in callback and client_aliases properties on the client. (see below for nested schema)
                            WS-Fed (WIF) addon indicator. Actual configuration is stored in `callback` and `client_aliases` properties on the client.
                          items:
                            type: object
                          type: array
                        zendesk:
                          description: |-
                            (Block List, Max: 1) Zendesk SSO configuration. (see below for nested schema)
                            Zendesk SSO configuration.
                          items:
                            properties:
                              accountName:
                                description: |-
                                  org.blob.core.windows.net would be the account name acme-org.
                                  Zendesk account name. Usually the first segment in your Zendesk URL, for example `https://acme-org.zendesk.com` would be `acme-org`.
                                type: string
                            type: object
                          type: array
                        zoom:
                          description: |-
                            (Block List, Max: 1) Zoom SSO configuration. (see below for nested schema)
                            Zoom SSO configuration.
                          items:
                            properties:
                              account:
                                description: |-
                                  (String) Your New Relic Account ID found in your New Relic URL after the /accounts/ path, for example https://rpm.newrelic.com/accounts/123456/query would be 123456.
                                  Zoom account name. Usually the first segment of your Zoom URL, for example `https://acme-org.zoom.us` would be `acme-org`.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  allowedClients:
                    description: |-
                      (List of String) List of applications ID's that will be allowed to make delegation request. By default, all applications will be allowed.
                      List of applications ID's that will be allowed to make delegation request. By default, all applications will be allowed.
                    items:
                      type: string
                    type: array
                  allowedLogoutUrls:
                    description: |-
                      (List of String) URLs that Auth0 may redirect to after logout.
                      URLs that Auth0 may redirect to after logout.
                    items:
                      type: string
                    type: array
                  allowedOrigins:
                    description: |-
                      origin resource sharing. By default, all your callback URLs will be allowed.
                      URLs that represent valid origins for cross-origin resource sharing. By default, all your callback URLs will be allowed.
                    items:
                      type: string
                    type: array
                  appType:
                    description: |-
                      (String) Type of application the client represents. Possible values are: native, spa, regular_web, non_interactive, sso_integration. Specific SSO integrations types accepted as well are: rms, box, cloudbees, concur, dropbox, mscrm, echosign, egnyte, newrelic, office365, salesforce, sentry, sharepoint, slack, springcm, zendesk, zoom.
                      Type of application the client represents. Possible values are: `native`, `spa`, `regular_web`, `non_interactive`, `sso_integration`. Specific SSO integrations types accepted as well are: `rms`, `box`, `cloudbees`, `concur`, `dropbox`, `mscrm`, `echosign`, `egnyte`, `newrelic`, `office365`, `salesforce`, `sentry`, `sharepoint`, `slack`, `springcm`, `zendesk`, `zoom`.
                    type: string
                  callbacks:
                    description: |-
                      (List of String) URLs that Auth0 may call back to after a user authenticates for the client. Make sure to specify the protocol (https://) otherwise the callback may fail in some cases. With the exception of custom URI schemes for native clients, all callbacks should use protocol https://.
                      URLs that Auth0 may call back to after a user authenticates for the client. Make sure to specify the protocol (https://) otherwise the callback may fail in some cases. With the exception of custom URI schemes for native clients, all callbacks should use protocol https://.
                    items:
                      type: string
                    type: array
                  clientAliases:
                    description: |-
                      (List of String) List of audiences/realms for SAML protocol. Used by the wsfed addon.
                      List of audiences/realms for SAML protocol. Used by the wsfed addon.
                    items:
                      type: string
                    type: array
                  clientId:
                    description: |-
                      (String) The ID of the client.
                      The ID of the client.
                    type: string
                  clientMetadata:
                    additionalProperties:
                      type: string
                    description: 'Metadata associated with the client, in the form
                      of an object with string values (max 255 chars). Maximum of
                      10 metadata properties allowed. Field names (max 255 chars)
                      are alphanumeric and may only include the following special
                      characters: `:,-+=_*?"/\()<>@ [Tab] [Space]`.'
                    type: object
                    x-kubernetes-map-type: granular
                  complianceLevel:
                    description: |-
                      (String) Defines the compliance level for this client, which may restrict it's capabilities. Can be one of none, fapi1_adv_pkj_par, fapi1_adv_mtls_par.
                      Defines the compliance level for this client, which may restrict it's capabilities. Can be one of `none`, `fapi1_adv_pkj_par`, `fapi1_adv_mtls_par`.
                    type: string
                  crossOriginAuth:
                    description: |-
                      origin authentication requests (true) or it is not allowed to make such requests (false).
                      Whether this client can be used to make cross-origin authentication requests (`true`) or it is not allowed to make such requests (`false`).
                    type: boolean
                  crossOriginLoc:
                    description: |-
                      origin verification takes place for the cross-origin auth flow when performing authentication in your own domain instead of Auth0 Universal Login page.
                      URL of the location in your site where the cross-origin verification takes place for the cross-origin auth flow when performing authentication in your own domain instead of Auth0 Universal Login page.
                    type: string
                  customLoginPage:
                    description: |-
                      (String) The content (HTML, CSS, JS) of the custom login page.
                      The content (HTML, CSS, JS) of the custom login page.
                    type: string
                  customLoginPageOn:
                    description: |-
                      (Boolean) Indicates whether a custom login page is to be used.
                      Indicates whether a custom login page is to be used.
                    type: boolean
                  defaultOrganization:
                    description: |-
                      (Block List, Max: 1) Configure and associate an organization with the Client (see below for nested schema)
                      Configure and associate an organization with the Client
                    items:
                      properties:
                        disable:
                          description: |-
                            (Boolean) If set, the default_organization will be removed.
                            If set, the `default_organization` will be removed.
                          type: boolean
                        flows:
                          description: |-
                            (List of String) Definition of the flow that needs to be configured. Eg. client_credentials
                            Definition of the flow that needs to be configured. Eg. client_credentials
                          items:
                            type: string
                          type: array
                        organizationId:
                          description: |-
                            (String) The unique identifier of the organization
                            The unique identifier of the organization
                          type: string
                      type: object
                    type: array
                  description:
                    description: |-
                      (String) Description of the purpose of the client.
                      Description of the purpose of the client.
                    type: string
                  encryptionKey:
                    additionalProperties:
                      type: string
                    description: |-
                      Fed responses with this client.
                      Encryption used for WS-Fed responses with this client.
                    type: object
                    x-kubernetes-map-type: granular
                  formTemplate:
                    description: |-
                      Federation.
                      HTML form template to be used for WS-Federation.
                    type: string
                  grantTypes:
                    description: |-
                      (List of String) Types of grants that this client is authorized to use.
                      Types of grants that this client is authorized to use.
                    items:
                      type: string
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  initiateLoginUri:
                    description: |-
                      (String) Initiate login URI. Must be HTTPS or an empty string.
                      Initiate login URI. Must be HTTPS or an empty string.
                    type: string
                  isFirstParty:
                    description: |-
                      party client.Defaults to true from the API
                      Indicates whether this client is a first-party client.Defaults to true from the API
                    type: boolean
                  isTokenEndpointIpHeaderTrusted:
                    description: |-
                      (Boolean) Indicates whether the token endpoint IP header is trusted. Requires the authentication method to be set to client_secret_post or client_secret_basic. Setting this property when creating the resource, will default the authentication method to client_secret_post. To change the authentication method to client_secret_basic use the auth0_client_credentials resource.
                      Indicates whether the token endpoint IP header is trusted. Requires the authentication method to be set to `client_secret_post` or `client_secret_basic`. Setting this property when creating the resource, will default the authentication method to `client_secret_post`. To change the authentication method to `client_secret_basic` use the `auth0_client_credentials` resource.
                    type: boolean
                  jwtConfiguration:
                    description: |-
                      (Block List, Max: 1) Configuration settings for the JWTs issued for this client. (see below for nested schema)
                      Configuration settings for the JWTs issued for this client.
                    items:
                      properties:
                        alg:
                          description: |-
                            (String) Algorithm used to sign JWTs. Can be one of HS256, RS256, PS256.
                            Algorithm used to sign JWTs. Can be one of `HS256`, `RS256`, `PS256`.
                          type: string
                        lifetimeInSeconds:
                          description: |-
                            (Number) AWS token lifetime in seconds.
                            Number of seconds during which the JWT will be valid.
                          type: number
                        scopes:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Permissions (scopes) included in JWTs.
                            Permissions (scopes) included in JWTs.
                          type: object
                          x-kubernetes-map-type: granular
                        secretEncoded:
                          description: |-
                            encoded.
                            Indicates whether the client secret is Base64-encoded.
                          type: boolean
                      type: object
                    type: array
                  logoUri:
                    description: |-
                      (String) URL of the logo for the client. Recommended size is 150px x 150px. If none is set, the default badge for the application type will be shown.
                      URL of the logo for the client. Recommended size is 150px x 150px. If none is set, the default badge for the application type will be shown.
                    type: string
                  mobile:
                    description: |-
                      (Block List, Max: 1) Additional configuration for native mobile apps. (see below for nested schema)
                      Additional configuration for native mobile apps.
                    items:
                      properties:
                        android:
                          description: |-
                            (Block List, Max: 1) Configuration settings for Android native apps. (see below for nested schema)
                            Configuration settings for Android native apps.
                          items:
                            properties:
                              appPackageName:
                                description: (String)
                                type: string
                              sha256CertFingerprints:
                                description: (List of String)
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        ios:
                          description: |-
                            (Block List, Max: 1) Configuration settings for i0S native apps. (see below for nested schema)
                            Configuration settings for i0S native apps.
                          items:
                            properties:
                              appBundleIdentifier:
                                description: (String)
                                type: string
                              teamId:
                                description: (String)
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  nativeSocialLogin:
                    description: |-
                      (Block List, Max: 1) Configuration settings to toggle native social login for mobile native applications. Once this is set it must stay set, with both resources set to false in order to change the app_type. (see below for nested schema)
                      Configuration settings to toggle native social login for mobile native applications. Once this is set it must stay set, with both resources set to `false` in order to change the `app_type`.
                    items:
                      properties:
                        apple:
                          description: '(Block List, Max: 1) (see below for nested
                            schema)'
                          items:
                            properties:
                              enabled:
                                description: (Boolean)
                                type: boolean
                            type: object
                          type: array
                        facebook:
                          description: '(Block List, Max: 1) (see below for nested
                            schema)'
                          items:
                            properties:
                              enabled:
                                description: (Boolean)
                                type: boolean
                            type: object
                          type: array
                        google:
                          description: '(Block List, Max: 1) (see below for nested
                            schema)'
                          items:
                            properties:
                              enabled:
                                description: (Boolean)
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  oidcBackchannelLogoutUrls:
                    description: |-
                      (Set of String, Deprecated) Set of URLs that are valid to call back from Auth0 for OIDC backchannel logout. Currently only one URL is allowed.
                      Set of URLs that are valid to call back from Auth0 for OIDC backchannel logout. Currently only one URL is allowed.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  oidcConformant:
                    description: |-
                      (Boolean) Indicates whether this client will conform to strict OIDC specifications.
                      Indicates whether this client will conform to strict OIDC specifications.
                    type: boolean
                  oidcLogout:
                    description: |-
                      (Block List, Max: 1) Configure OIDC logout for the Client (see below for nested schema)
                      Configure OIDC logout for the Client
                    items:
                      properties:
                        backchannelLogoutInitiators:
                          description: |-
                            (Block List, Max: 1) Configure OIDC logout initiators for the Client (see below for nested schema)
                            Configure OIDC logout initiators for the Client
                          items:
                            properties:
                              mode:
                                description: |-
                                  (String) Determines the configuration method for enabling initiators. custom enables only the initiators listed in the backchannel_logout_selected_initiators set, all enables all current and future initiators.
                                  Determines the configuration method for enabling initiators. `custom` enables only the initiators listed in the backchannel_logout_selected_initiators set, `all` enables all current and future initiators.
                                type: string
                              selectedInitiators:
                                description: |-
                                  (Set of String) Contains the list of initiators to be enabled for the given client.
                                  Contains the list of initiators to be enabled for the given client.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        backchannelLogoutUrls:
                          description: |-
                            (Set of String) Set of URLs that are valid to call back from Auth0 for OIDC backchannel logout. Currently only one URL is allowed.
                            Set of URLs that are valid to call back from Auth0 for OIDC backchannel logout. Currently only one URL is allowed.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  organizationRequireBehavior:
                    description: |-
                      (String) Defines how to proceed during an authentication transaction when organization_usage = "require". Can be no_prompt (default), pre_login_prompt or  post_login_prompt.
                      Defines how to proceed during an authentication transaction when `organization_usage = "require"`. Can be `no_prompt` (default), `pre_login_prompt` or  `post_login_prompt`.
                    type: string
                  organizationUsage:
                    description: |-
                      (String) Defines how to proceed during an authentication transaction with regards to an organization. Can be deny (default), allow or require.
                      Defines how to proceed during an authentication transaction with regards to an organization. Can be `deny` (default), `allow` or `require`.
                    type: string
                  refreshToken:
                    description: |-
                      (Block List, Max: 1) Configuration settings for the refresh tokens issued for this client. (see below for nested schema)
                      Configuration settings for the refresh tokens issued for this client.
                    items:
                      properties:
                        expirationType:
                          description: |-
                            expiring. Whether a refresh token will expire based on an absolute lifetime, after which the token can no longer be used. If rotation is rotating, this must be set to expiring.
                            Options include `expiring`, `non-expiring`. Whether a refresh token will expire based on an absolute lifetime, after which the token can no longer be used. If rotation is `rotating`, this must be set to `expiring`.
                          type: string
                        idleTokenLifetime:
                          description: |-
                            (Number) The time in seconds after which inactive refresh tokens will expire.
                            The time in seconds after which inactive refresh tokens will expire.
                          type: number
                        infiniteIdleTokenLifetime:
                          description: |-
                            (Boolean) Whether inactive refresh tokens should remain valid indefinitely.
                            Whether inactive refresh tokens should remain valid indefinitely.
                          type: boolean
                        infiniteTokenLifetime:
                          description: |-
                            (Boolean) Whether refresh tokens should remain valid indefinitely. If false, token_lifetime should also be set.
                            Whether refresh tokens should remain valid indefinitely. If false, `token_lifetime` should also be set.
                          type: boolean
                        leeway:
                          description: |-
                            (Number) The amount of time in seconds in which a refresh token may be reused without triggering reuse detection.
                            The amount of time in seconds in which a refresh token may be reused without triggering reuse detection.
                          type: number
                        policies:
                          description: |-
                            resource refresh token exchange (MRRT), defining how refresh tokens can be used across different resource servers (see below for nested schema)
                            A collection of policies governing multi-resource refresh token exchange (MRRT), defining how refresh tokens can be used across different resource servers
                          items:
                            properties:
                              audience:
                                description: |-
                                  (String) Audience of the SAML Assertion. Default will be the Issuer on SAMLRequest.
                                  The identifier of the resource server to which the Multi Resource Refresh Token Policy applies
                                type: string
                              scope:
                                description: |-
                                  (String) Requested scope for SAP APIs.
                                  The resource server permissions granted under the Multi Resource Refresh Token Policy, defining the context in which an access token can be used
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        rotationType:
                          description: |-
                            rotating. When rotating, exchanging a refresh token will cause a new refresh token to be issued and the existing token will be invalidated. This allows for automatic detection of token reuse if the token is leaked.
                            Options include `rotating`, `non-rotating`. When `rotating`, exchanging a refresh token will cause a new refresh token to be issued and the existing token will be invalidated. This allows for automatic detection of token reuse if the token is leaked.
                          type: string
                        tokenLifetime:
                          description: |-
                            (Number) The absolute lifetime of a refresh token in seconds.
                            The absolute lifetime of a refresh token in seconds.
                          type: number
                      type: object
                    type: array
                  requireProofOfPossession:
                    description: |-
                      of-Possession mandatory for this client.
                      Makes the use of Proof-of-Possession mandatory for this client.
                    type: boolean
                  requirePushedAuthorizationRequests:
                    description: |-
                      (Boolean) Makes the use of Pushed Authorization Requests mandatory for this client. This feature currently needs to be enabled on the tenant in order to make use of it.
                      Makes the use of Pushed Authorization Requests mandatory for this client. This feature currently needs to be enabled on the tenant in order to make use of it.
                    type: boolean
                  sessionTransfer:
                    description: '(Block List, Max: 1) (see below for nested schema)'
                    items:
                      properties:
                        allowRefreshToken:
                          description: |-
                            (Boolean) Indicates whether the application is allowed to use a refresh token when using a session_transfer_token session.
                            Indicates whether the application is allowed to use a refresh token when using a session_transfer_token session.
                          type: boolean
                        allowedAuthenticationMethods:
                          description: (Set of String)
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        canCreateSessionTransferToken:
                          description: |-
                            (Boolean) Indicates whether the application(Native app) can use the Token Exchange endpoint to create a session_transfer_token
                            Indicates whether the application(Native app) can use the Token Exchange endpoint to create a session_transfer_token
                          type: boolean
                        enforceDeviceBinding:
                          description: |-
                            (String) Configures the level of device binding enforced when a session_transfer_token is consumed. Can be one of ip, asn or none.
                            Configures the level of device binding enforced when a session_transfer_token is consumed. Can be one of `ip`, `asn` or `none`.
                          type: string
                      type: object
                    type: array
                  sso:
                    description: |-
                      On (true) or whether the identity provider will (false).
                      Applies only to SSO clients and determines whether Auth0 will handle Single Sign-On (true) or whether the identity provider will (false).
                    type: boolean
                  ssoDisabled:
                    description: |-
                      (Boolean) Indicates whether or not SSO is disabled.
                      Indicates whether or not SSO is disabled.
                    type: boolean
                  tokenExchange:
                    description: |-
                      (Block List, Max: 1) Allows configuration for token exchange (see below for nested schema)
                      Allows configuration for token exchange
                    items:
                      properties:
                        allowAnyProfileOfType:
                          description: |-
                            (List of String) List of allowed profile types for token exchange
                            List of allowed profile types for token exchange
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  tokenQuota:
                    description: |-
                      (Block List, Max: 1) The token quota configuration. (see below for nested schema)
                      The token quota configuration.
                    items:
                      properties:
                        clientCredentials:
                          description: |-
                            (Block List, Min: 1, Max: 1) The token quota configuration for client credentials. (see below for nested schema)
                            The token quota configuration for client credentials.
                          items:
                            properties:
                              enforce:
                                description: |-
                                  (Boolean) If enabled, the quota will be enforced and requests in excess of the quota will fail. If disabled, the quota will not be enforced, but notifications for requests exceeding the quota will be available in logs.
                                  If enabled, the quota will be enforced and requests in excess of the quota will fail. If disabled, the quota will not be enforced, but notifications for requests exceeding the quota will be available in logs.
                                type: boolean
                              perDay:
                                description: |-
                                  (Number) Maximum number of issued tokens per day
                                  Maximum number of issued tokens per day
                                type: number
                              perHour:
                                description: |-
                                  (Number) Maximum number of issued tokens per hour
                                  Maximum number of issued tokens per hour
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  webOrigins:
                    description: |-
                      (List of String) URLs that represent valid web origins for use with web message response mode.
                      URLs that represent valid web origins for use with web message response mode.
                    items:
                      type: string
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
